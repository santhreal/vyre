# Duplicated source lines per crate.
#
# A line counts as duplicated when an 8-line normalized shingle covering it
# also appears in another file. More than the pin fails; less is reported so
# the owning dedup PR can lower it here as part of its own diff.
#
# A pin is never raised. `--write-baseline` rewrites every row from the current
# tree and drops this block, so it must not be used to record one crate's
# progress; edit that crate's row by hand. A crate with duplication and no row
# fails, so a new crate cannot arrive unmeasured.
#
# vyre-primitives and vyre-libs sit at their measured values but not at a floor:
# the iterative-solver and CSR traversal builders in vyre-primitives duplicate
# against their vyre-libs dispatch partners, and neither side owns the shape yet.
#
# Eleven of vyre-primitives' lines are deliberate and cannot be collapsed.
# `vyre_primitives::types::linear_check::LinearDiscipline` and
# `vyre_foundation::ir::LinearType` declare the same four substructural
# disciplines and the same `forbids_drop` / `forbids_reuse` predicates. The
# primitive layer builds with no IR dependency at all, since the
# `vyre-foundation` edge is optional and absent under `--no-default-features`,
# and a foundation-layer crate cannot import from the primitives layer, so
# neither direction has a legal owner. The pair is pinned, not fixed.

[[crate]]
name = "conform"
duplicate_lines = 765
total_lines = 10298

[[crate]]
name = "examples"
duplicate_lines = 37
total_lines = 490

[[crate]]
name = "fuzz"
duplicate_lines = 23
total_lines = 106

[[crate]]
name = "structure-gate"
duplicate_lines = 0
total_lines = 1943

[[crate]]
name = "tests"
duplicate_lines = 8
total_lines = 2656

[[crate]]
name = "vyre"
duplicate_lines = 0
total_lines = 248

[[crate]]
name = "vyre-aot"
duplicate_lines = 31
total_lines = 1511

[[crate]]
name = "vyre-bench"
duplicate_lines = 2161
total_lines = 37643

[[crate]]
name = "vyre-debug"
duplicate_lines = 9
total_lines = 2227

[[crate]]
name = "vyre-driver"
duplicate_lines = 1047
total_lines = 36080

[[crate]]
name = "vyre-driver-cuda"
duplicate_lines = 2976
total_lines = 67502

[[crate]]
name = "vyre-driver-metal"
duplicate_lines = 251
total_lines = 4706

[[crate]]
name = "vyre-driver-reference"
duplicate_lines = 23
total_lines = 655

[[crate]]
name = "vyre-driver-spirv"
duplicate_lines = 118
total_lines = 1913

[[crate]]
name = "vyre-driver-wgpu"
duplicate_lines = 3900
total_lines = 56824

[[crate]]
name = "vyre-emit-metal"
duplicate_lines = 22
total_lines = 766

[[crate]]
name = "vyre-emit-naga"
duplicate_lines = 221
total_lines = 8993

[[crate]]
name = "vyre-emit-ptx"
duplicate_lines = 105
total_lines = 10958

[[crate]]
name = "vyre-emit-spirv"
duplicate_lines = 43
total_lines = 1139

[[crate]]
name = "vyre-foundation"
duplicate_lines = 4342
total_lines = 95039

[[crate]]
name = "vyre-frontend-rust"
duplicate_lines = 138
total_lines = 5228

[[crate]]
name = "vyre-grammar-gen"
duplicate_lines = 119
total_lines = 3383

[[crate]]
name = "vyre-libs"
duplicate_lines = 11446
total_lines = 187480

[[crate]]
name = "vyre-lints"
duplicate_lines = 0
total_lines = 3438

[[crate]]
name = "vyre-lower"
duplicate_lines = 197
total_lines = 13201

[[crate]]
name = "vyre-macros"
duplicate_lines = 41
total_lines = 2070

[[crate]]
name = "vyre-megakernel"
duplicate_lines = 8
total_lines = 3599

[[crate]]
name = "vyre-primitives"
duplicate_lines = 7008
total_lines = 102012

[[crate]]
name = "vyre-reference"
duplicate_lines = 743
total_lines = 15256

[[crate]]
name = "vyre-runtime"
duplicate_lines = 314
total_lines = 29113

[[crate]]
name = "vyre-pass-engine"
duplicate_lines = 288
total_lines = 9257

[[crate]]
name = "vyre-test-support"
duplicate_lines = 11
total_lines = 1115

[[crate]]
name = "vyre-spec"
duplicate_lines = 244
total_lines = 9259

[[crate]]
name = "xtask"
duplicate_lines = 102
total_lines = 17046

[[crate]]
name = "xtask-evidence"
duplicate_lines = 475
total_lines = 25648

[[crate]]
name = "xtask-registry"
duplicate_lines = 58
total_lines = 7767
