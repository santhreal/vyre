# Duplicated source lines per crate.
#
# A line counts as duplicated when an 8-line normalized shingle covering it
# also appears in another file. More than the pin fails; less is reported so
# the owning dedup PR can lower it here as part of its own diff.
#
# A pin is never raised. `--write-baseline` rewrites every row from the current
# tree and drops this block, so it must not be used to record one crate's
# progress; edit that crate's row by hand. A crate with duplication and no row
# fails, so a new crate cannot arrive unmeasured.
#
# vyre-primitives and vyre-libs sit at their measured values but not at a floor:
# the iterative-solver builders in vyre-primitives duplicate against their
# vyre-libs dispatch partners, and neither side owns the shape yet. The CSR
# frontier buffer layout is no longer part of that: `graph::program_graph` owns
# `word_buffer`, `frontier_buffer` and `push_frontier_changed_buffers`, and every
# graph primitive declares its own buffers through them.
#
# Eleven of vyre-primitives' lines are deliberate and cannot be collapsed.
# `vyre_primitives::types::linear_check::LinearDiscipline` and
# `vyre_foundation::ir::LinearType` declare the same four substructural
# disciplines and the same `forbids_drop` / `forbids_reuse` predicates. The
# primitive layer builds with no IR dependency at all, since the
# `vyre-foundation` edge is optional and absent under `--no-default-features`,
# and a foundation-layer crate cannot import from the primitives layer, so
# neither direction has a legal owner. The pair is pinned, not fixed.

[[crate]]
name = "conform"
duplicate_lines = 367
total_lines = 9891

[[crate]]
name = "examples"
duplicate_lines = 37
total_lines = 490

[[crate]]
name = "fuzz"
duplicate_lines = 23
total_lines = 106

[[crate]]
name = "structure-gate"
duplicate_lines = 0
total_lines = 1943

[[crate]]
name = "tests"
duplicate_lines = 8
total_lines = 2857

[[crate]]
name = "vyre"
duplicate_lines = 0
total_lines = 248

[[crate]]
name = "vyre-aot"
duplicate_lines = 31
total_lines = 1511

[[crate]]
name = "vyre-bench"
duplicate_lines = 1846
total_lines = 37763

[[crate]]
name = "vyre-debug"
duplicate_lines = 9
total_lines = 2227

[[crate]]
name = "vyre-driver"
duplicate_lines = 1047
total_lines = 36080

[[crate]]
name = "vyre-driver-cuda"
duplicate_lines = 3145
total_lines = 67711

[[crate]]
name = "vyre-driver-metal"
duplicate_lines = 175
total_lines = 4706

[[crate]]
name = "vyre-driver-reference"
duplicate_lines = 23
total_lines = 655

[[crate]]
name = "vyre-driver-spirv"
duplicate_lines = 168
total_lines = 1938

[[crate]]
name = "vyre-driver-wgpu"
duplicate_lines = 3974
total_lines = 56975

[[crate]]
name = "vyre-emit-metal"
duplicate_lines = 22
total_lines = 766

[[crate]]
name = "vyre-emit-naga"
duplicate_lines = 221
total_lines = 8993

[[crate]]
name = "vyre-emit-ptx"
duplicate_lines = 105
total_lines = 10958

[[crate]]
name = "vyre-emit-spirv"
duplicate_lines = 43
total_lines = 1139

[[crate]]
name = "vyre-foundation"
duplicate_lines = 4127
total_lines = 94879

[[crate]]
name = "vyre-frontend-rust"
duplicate_lines = 138
total_lines = 5228

[[crate]]
name = "vyre-grammar-gen"
duplicate_lines = 26
total_lines = 3276

[[crate]]
name = "vyre-libs"
duplicate_lines = 11425
total_lines = 187461

[[crate]]
name = "vyre-lints"
duplicate_lines = 0
total_lines = 3438

[[crate]]
name = "vyre-lower"
duplicate_lines = 197
total_lines = 13201

[[crate]]
name = "vyre-macros"
duplicate_lines = 41
total_lines = 2070

[[crate]]
name = "vyre-megakernel"
duplicate_lines = 8
total_lines = 3599

[[crate]]
name = "vyre-primitives"
duplicate_lines = 6981
total_lines = 101992

[[crate]]
name = "vyre-reference"
duplicate_lines = 743
total_lines = 15256

[[crate]]
name = "vyre-runtime"
duplicate_lines = 314
total_lines = 29113

[[crate]]
name = "vyre-pass-engine"
duplicate_lines = 257
total_lines = 9082

[[crate]]
name = "vyre-test-support"
duplicate_lines = 11
total_lines = 1115

[[crate]]
name = "vyre-spec"
duplicate_lines = 84
total_lines = 9165

[[crate]]
name = "xtask"
duplicate_lines = 102
total_lines = 17046

[[crate]]
name = "xtask-evidence"
duplicate_lines = 475
total_lines = 25648

[[crate]]
name = "xtask-registry"
duplicate_lines = 58
total_lines = 8078
