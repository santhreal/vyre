# Duplicated source lines per crate.
#
# A line counts as duplicated when an 8-line normalized shingle covering it
# also appears in another file. More than the pin fails; less is reported so
# the owning dedup PR can lower it here as part of its own diff.
#
# A pin is never raised. `--write-baseline` adds a row for a crate that has none
# and touches nothing else, so it cannot erase the intentional-red state of a
# pin whose closing work another checkout owns. Lowering is
# `--lower-pin CRATE`, one row, in the diff that removed the duplication; it
# refuses to move a pin up. A crate with duplication and no row fails, so a new
# crate cannot arrive unmeasured.
#
# vyre-libs sits at a measured value but not at a floor: the iterative-solver
# builders duplicate against their dispatch partners, and neither side owns the
# shape yet. Its pin is below its measurement because the composition move
# relabelled duplication that had been attributed to vyre-primitives without
# moving the allowance with the files. The CSR frontier buffer layout is no
# longer part of that: `graph::program_graph` owns `word_buffer`,
# `frontier_buffer` and `push_frontier_changed_buffers`, and every graph
# primitive declares its own buffers through them. Nor is the kernel-body
# skeleton below those buffers: `graph::frontier_bits` owns packed-bitset word
# and mask addressing, `graph::lane_grid` owns the one-dimensional dispatch grid,
# and `reduce::workgroup_tree::hillis_steele_inclusive_sum_nodes` owns the
# double-buffered workgroup scan sweep.
#
# The eleven vyre-primitives lines that were pinned as deliberate are gone.
# `vyre_primitives::types::linear_check::LinearDiscipline` restated
# `vyre_foundation::ir::LinearType`, including `forbids_drop` and
# `forbids_reuse`, and the pair was recorded here as unownable because the
# primitive layer builds with no IR dependency. It had no callers, so the copy
# was deleted rather than pinned, and `vyre_foundation::ir` is the one owner.

[[crate]]
name = "conform"
duplicate_lines = 83
total_lines = 12302

[[crate]]
name = "examples"
duplicate_lines = 18
total_lines = 438

[[crate]]
name = "fuzz"
duplicate_lines = 23
total_lines = 106

[[crate]]
name = "structure-gate"
duplicate_lines = 0
total_lines = 1955

[[crate]]
name = "tests"
duplicate_lines = 0
total_lines = 5031

[[crate]]
name = "vyre"
duplicate_lines = 0
total_lines = 248

[[crate]]
name = "vyre-aot"
duplicate_lines = 21
total_lines = 1513

[[crate]]
name = "vyre-bench"
duplicate_lines = 980
total_lines = 36934

[[crate]]
name = "vyre-debug"
duplicate_lines = 9
total_lines = 2227

[[crate]]
name = "vyre-driver"
duplicate_lines = 487
total_lines = 39545

[[crate]]
name = "vyre-driver-cuda"
duplicate_lines = 1637
total_lines = 66564

[[crate]]
name = "vyre-driver-metal"
duplicate_lines = 27
total_lines = 4438

[[crate]]
name = "vyre-driver-reference"
duplicate_lines = 23
total_lines = 655

[[crate]]
name = "vyre-driver-spirv"
duplicate_lines = 34
total_lines = 1795

[[crate]]
name = "vyre-driver-wgpu"
duplicate_lines = 294
total_lines = 31937

[[crate]]
name = "vyre-emit-metal"
duplicate_lines = 21
total_lines = 770

[[crate]]
name = "vyre-emit-naga"
duplicate_lines = 121
total_lines = 9078

[[crate]]
name = "vyre-emit-ptx"
duplicate_lines = 69
total_lines = 11943

[[crate]]
name = "vyre-emit-spirv"
duplicate_lines = 16
total_lines = 1127

[[crate]]
name = "vyre-foundation"
duplicate_lines = 2423
total_lines = 104436

[[crate]]
name = "vyre-libs"
duplicate_lines = 6135
total_lines = 213050

[[crate]]
name = "vyre-lints"
duplicate_lines = 0
total_lines = 3438

[[crate]]
name = "vyre-lower"
duplicate_lines = 69
total_lines = 16981

[[crate]]
name = "vyre-macros"
duplicate_lines = 41
total_lines = 2070

[[crate]]
name = "vyre-megakernel"
duplicate_lines = 8
total_lines = 3605

[[crate]]
name = "vyre-primitives"
duplicate_lines = 0
total_lines = 3838

[[crate]]
name = "vyre-reference"
duplicate_lines = 394
total_lines = 30672

[[crate]]
name = "vyre-runtime"
duplicate_lines = 189
total_lines = 32328

[[crate]]
name = "vyre-pass-engine"
duplicate_lines = 170
total_lines = 7507

[[crate]]
name = "vyre-test-support"
duplicate_lines = 0
total_lines = 3006

[[crate]]
name = "vyre-spec"
duplicate_lines = 37
total_lines = 10596

[[crate]]
name = "xtask"
duplicate_lines = 0
total_lines = 29798

[[crate]]
name = "xtask-evidence"
duplicate_lines = 34
total_lines = 27563

[[crate]]
name = "xtask-registry"
duplicate_lines = 0
total_lines = 8429
