# `vyre-libs-graph`

Graph algorithms, CSR traversal, AST walks, dominator trees, and topological sort.

<!-- BEGIN GENERATED CRATE CONTRACT -->
## Crate contract

This section is generated by `xtask crate-readmes --write` from
the crate manifest, release train, ownership registry, and crate-guide metadata.

### Purpose

Own graph algorithms, CSR traversal, AST walks, dominator trees, and topological sorting IR compositions. Does not own pattern matching engines, backend lowering, or runtime execution.

### Boundaries

This crate is the `libraries` layer's `libs-graph` seam, at `vyre-libs-graph`.
Every production dependency on it crosses Graph algorithm compositions: CSR traversal, AST walks, dominator trees and topological sort.
The layer ranks in the architecture manifest decide which layers may
reach it; a dependency from a layer that does not outrank this one requires
an architecture-manifest change.

### Minimal real example

Run the checked-in behavior from `vyre-libs-graph/tests/adaptive_four_russians_dense_generated.rs`:

```console
./cargo_full test -p vyre-libs-graph --test adaptive_four_russians_dense_generated
```

### Features

- Manifest features: `default`, `dense-reachability`, `graph`, `graph-dispatch`, `impact-mask`, `interactive-graphics`, `topology`
- Default feature members: `graph`

### Errors and unsupported behavior

Invalid shapes, unsupported compositions, arithmetic overflow, and malformed evidence are rejected by the builder or validator before dispatch.

### Testing

See [`docs/testing/vyre-libs-graph.md`](../docs/testing/vyre-libs-graph.md) for the crate's test command,
hardware contract, expected skips, and failure semantics. It is generated
from `docs/testing/TESTING.toml`, which is authoritative.

### Release status

`vyre-libs-graph@0.8.0` is a publishable crate on the current Vyre release train. Publication still requires the release evidence and user-approval gates.

### Ownership

[`docs/CRATE_OWNERSHIP.toml`](../docs/CRATE_OWNERSHIP.toml) is authoritative for this crate's
responsibility, layer, and seam.

### License

Licensed under either of

- Apache License, Version 2.0, or
- MIT license

at your option. See the workspace `LICENSE-APACHE` and `LICENSE-MIT` files.

<!-- END GENERATED CRATE CONTRACT -->
