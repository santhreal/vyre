# The architecture manifest: what every workspace member is for, and the
# direction its production dependencies may point.
#
# There are two authorities and they do not overlap. Cargo owns the actual
# package and feature graph: which edge exists, its kind, whether it is
# optional, which feature activates it, and which target condition it is
# declared under. This file owns intent: one row per member stating its layer,
# the named seam it exposes, the one documented interface every production
# edge into it crosses, and what it is responsible for. Nothing here restates
# a fact cargo already carries: the publication class lives beside `publish`
# in each member's own `[package.metadata.vyre]` table, and the edge set lives
# in the dependency tables.
#
# Schema 3 carried one `[[crate.dependency]]` row per internal production edge,
# with the destination's features, target conditions, kinds, optionality and
# default-feature setting copied out of the manifests. Holding 328 copies of a
# cargo fact exact is work no reader benefits from, and it bought a description
# per consumer rather than per seam: 27 destinations were described by more
# than one interface text and 10 were declared public by one consumer and
# private by another. One row per member states the seam once.
#
# The `crate-ownership` gate joins the two authorities. It resolves every
# internal edge under the union of every feature, so an edge a feature
# activates is judged like a required one, and it holds each edge to the layer
# ranks, the seam a destination declares, the layers a destination's layer
# admits, the facade export set and the publication closure.

schema_version = 4

# The layer DAG, and the whole of the direction contract.
#
# Two rules over one graph, and a production edge is legal only when both
# pass.
#
# `rank` is dependency depth: the longest chain of cross-layer production edges
# from a layer down to one that depends on nothing outside itself. An edge
# points down when the source rank is strictly greater than the destination
# rank. That single comparison rejects a reversal and makes a cross-layer cycle
# unrepresentable, so there is no roster of permitted layer pairs to keep in
# agreement with the manifests.
#
# Two layers share a rank when neither depends on the other. A new edge between
# two such layers fails the direction rule, which is the decision worth
# stopping on: one of the two ranks is then wrong and the manifest records
# which.
#
# `consumed_by` closes what rank leaves open. Rank permits every edge that
# points down and does not judge an edge inside one layer at all, so a new
# direct dependency could expand rebuild fan-out or pull a composition into a
# driver while both endpoints kept a plausible local description. Every layer
# states the closed set of consumer layers admitted to it, its own name
# included when its members reach each other. An edge from a layer the
# destination does not admit fails, and an admitted layer no production edge
# crosses is a stale declaration and fails too, so the set is exactly the layer
# pairs the manifests cross.

[[layer]]
name = "foundation"
rank = 0
purpose = "Typed IR, logical domains, specification records, diagnostics, and the derive macros that generate them."
# Reached by every layer in the workspace, and by its own members.
consumed_by = [
    "backend-neutral",
    "compiler-boundary",
    "concrete-backend",
    "conformance",
    "emitter",
    "facade",
    "foundation",
    "libraries",
    "lowering",
    "packaging",
    "pass-engine",
    "primitives",
    "registry-link",
    "runtime",
    "semantics",
    "test-tooling",
    "tooling",
]

[[layer]]
name = "standalone-tooling"
rank = 0
purpose = "Source-structure gates that read the tree and depend on no compiler crate."
# A gate that must keep answering while the workspace does not compile is
# read by the gate runners and the fixtures that resolve a checkout, and by
# nothing that ships.
consumed_by = [
    "test-tooling",
    "tooling",
]

[[layer]]
name = "lowering"
rank = 1
purpose = "One verified selected-module representation between semantic IR and target emission."
# The selected module is produced at the compiler boundary and consumed by the
# emitters and the backends that load what they emit.
consumed_by = [
    "compiler-boundary",
    "concrete-backend",
    "emitter",
    "registry-link",
    "tooling",
]

[[layer]]
name = "primitives"
rank = 1
purpose = "Intrinsic operations, each with its own emitter arm in every backend and its own reference arm."
# An intrinsic is composed into a library, interpreted by the oracle, or
# registered. A concrete backend reaches its emitter arms through the compiler
# boundary.
consumed_by = [
    "conformance",
    "libraries",
    "pass-engine",
    "registry-link",
    "semantics",
    "test-tooling",
    "tooling",
]

[[layer]]
name = "compiler-boundary"
rank = 2
purpose = "Schedule search, artifact identity, and authenticated target-payload construction."
# The one route from semantic IR to an artifact, so every layer above it that
# compiles anything crosses it.
consumed_by = [
    "backend-neutral",
    "concrete-backend",
    "conformance",
    "facade",
    "libraries",
    "packaging",
    "pass-engine",
    "registry-link",
    "runtime",
    "test-tooling",
    "tooling",
]

[[layer]]
name = "emitter"
rank = 2
purpose = "Target text and binary emission for one dialect each."
# A concrete backend emits through the dialect it loads. One emitter reaches
# another where a dialect is built on a second one, so the layer admits
# itself.
consumed_by = [
    "concrete-backend",
    "emitter",
    "tooling",
]

[[layer]]
name = "semantics"
rank = 2
purpose = "The independent semantic oracle. Never a production execution route."
# Reached by the backend that adapts it to the driver contract and by the
# parity harnesses that join its result to a device result. Nothing in the
# production compile path admits it.
consumed_by = [
    "concrete-backend",
    "conformance",
    "test-tooling",
    "tooling",
]

[[layer]]
name = "libraries"
rank = 3
purpose = "Domain-neutral compositions built from existing IR."
# Semantic-library policy is composed, not embedded. An emitter, a concrete
# backend, a packaging crate and the facade reach IR through the compiler
# boundary, never through a composition library. `backend-neutral` and
# `runtime` are here because each carries one feature-activated composition
# edge, and rows `61` and `68` own removing them.
consumed_by = [
    "backend-neutral",
    "conformance",
    "libraries",
    "pass-engine",
    "registry-link",
    "runtime",
    "tooling",
]

[[layer]]
name = "test-tooling"
rank = 5
purpose = "Shared verification fixtures and harness support, private to the workspace."
# No production consumer. A fixture crate is reached by a dev-dependency
# only, which cargo resolves in a separate graph.
consumed_by = []

[[layer]]
name = "backend-neutral"
rank = 4
purpose = "The dispatch, binding, and capability contract every concrete backend implements."
# Implemented by each concrete backend and consumed by everything that holds a
# device handle or a registration.
consumed_by = [
    "concrete-backend",
    "conformance",
    "facade",
    "packaging",
    "registry-link",
    "runtime",
    "test-tooling",
    "tooling",
]

[[layer]]
name = "pass-engine"
rank = 4
purpose = "Pass scheduling and rewrite application over semantic IR."
# Driven by the gates that prove a rewrite, and by nothing that ships.
consumed_by = [
    "tooling",
]

[[layer]]
name = "concrete-backend"
rank = 5
purpose = "One device family each: capability probing, module loading, and dispatch."
# Selected through the driver registry, so only the layers that link a
# registration or measure a device name one.
consumed_by = [
    "conformance",
    "facade",
    "registry-link",
    "tooling",
]

[[layer]]
name = "packaging"
rank = 5
purpose = "Ahead-of-time artifact packaging and load."
# No workspace member depends on it. An ahead-of-time package is produced for
# a consumer outside this workspace.
consumed_by = []

[[layer]]
name = "runtime"
rank = 5
purpose = "Submission, admission, tenancy, and lifecycle policy over a compiled artifact."
# Reached through the facade, and directly only by the harnesses and gates that
# submit work.
consumed_by = [
    "conformance",
    "facade",
    "tooling",
]

[[layer]]
name = "facade"
rank = 6
purpose = "The single curated consumer surface over the layers below it."
exports_declared_seams = true
# What a consumer outside the workspace compiles against. Inside it, only the
# parity harnesses and the gates.
consumed_by = [
    "conformance",
    "tooling",
]

[[layer]]
name = "registry-link"
rank = 6
purpose = "Link anchors that pull registration submissions into a final binary."
# Linked by the binaries that need a populated registry: the parity harnesses
# and the gate and benchmark binaries.
consumed_by = [
    "conformance",
    "tooling",
]

[[layer]]
name = "conformance"
rank = 7
purpose = "Parity harnesses that join a production result to an independently produced oracle result."
# The harness reaches its own schema crate, and nothing outside the layer
# reaches either.
consumed_by = [
    "conformance",
]

[[layer]]
name = "tooling"
rank = 7
purpose = "Gates, benchmarks, and generators that read the whole workspace."
# A gate binary reaches the gate registry and the evidence crates beside it,
# and nothing else reaches any of them.
consumed_by = [
    "tooling",
]

[[crate]]
package = "structure-gate"
path = "structure-gate"
layer = "standalone-tooling"
seam = "source-structure"
interface = "Whether a directory carries Rust source, which directory owns a domain, and the checkout root a gate reports on."
responsibility = "Enforce the crate roster, one operation identity per semantic operation, one home per concept, and one place per module. Depends on no vyre crate so it keeps running while the workspace does not compile."

[[crate]]
package = "vyre"
path = "vyre"
layer = "facade"
seam = "public-facade"
interface = "The curated public surface: IR, driver, runtime and the artifact compiler, re-exported."
responsibility = "Public facade. Re-export IR, driver, runtime, and the artifact compiler. Own no logic."

[[crate]]
package = "vyre-alloc-probe"
path = "vyre-alloc-probe"
layer = "standalone-tooling"
seam = "allocation-probe"
interface = "Per-thread heap counters installed as the global allocator."
responsibility = "Count heap traffic per thread behind a GlobalAlloc wrapper, so an allocation budget measures the code under test rather than the test schedule. Depends on no vyre crate, so a harness binary links it at run time and a driver suite links it as a dev-dependency."

[[crate]]
package = "vyre-aot"
path = "vyre-aot"
layer = "packaging"
seam = "aot-artifacts"
interface = "Ahead-of-time artifact packaging and load."
responsibility = "Package the same megakernel artifact class ahead of time. Not a second compile path. No workspace crate currently depends on this one."

[[crate]]
package = "vyre-bench"
path = "vyre-bench"
layer = "tooling"
seam = "workload-benchmarks"
interface = "Reproducible workload benchmarks and their recorded evidence."
responsibility = "Own reproducible workload benchmarks against the best available native baseline for each class, not against vyre's own unfused output."

[[crate]]
package = "vyre-conform"
path = "conform/vyre-conform"
layer = "conformance"
seam = "parity-harness"
interface = "Parity execution, counterexample minimization, law checks and versioned certificates."
responsibility = "Execute production artifacts against independent reference semantics, minimize counterexamples, check algebraic laws, and issue versioned certificates and replay records through one library and thin CLI."

[[crate]]
package = "vyre-conform-spec"
path = "conform/vyre-conform-spec"
layer = "conformance"
seam = "conformance-schema"
interface = "Versioned conformance case, result and certificate schemas."
responsibility = "Define conformance case, result, and certificate schemas against the public facade."

[[crate]]
package = "vyre-debug"
path = "vyre-debug"
layer = "tooling"
seam = "debugging"
interface = "Program and artifact inspection for a caller holding a compiled artifact."
responsibility = "Inspect, explain, and diagnose typed programs, lowering, and product-library composition."

[[crate]]
package = "vyre-driver"
path = "vyre-driver"
layer = "backend-neutral"
seam = "backend-contract"
interface = "The backend-neutral target, materialization, submission and completion contracts, and the driver registry every backend registers into."
responsibility = "Define backend-neutral device, target compiler registration, artifact materialization, binding, submission, completion, capability, dispatch, and evidence contracts."
facade_exported = true

[[crate]]
package = "vyre-driver-cuda"
path = "vyre-driver-cuda"
layer = "concrete-backend"
seam = "cuda-driver"
interface = "Native accelerator backend registration, capability probing and dispatch."
responsibility = "Own pure PTX target compilation, native device acquisition, materialization, dispatch, graphs, and release-path evidence."
facade_exported = true

[[crate]]
package = "vyre-driver-metal"
path = "vyre-driver-metal"
layer = "concrete-backend"
seam = "metal-driver"
interface = "Native Apple backend registration, capability probing and dispatch."
responsibility = "Own pure MSL target compilation, native Apple device acquisition, materialization, dispatch, and backend evidence."

[[crate]]
package = "vyre-driver-reference"
path = "vyre-driver-reference"
layer = "concrete-backend"
seam = "reference-driver"
interface = "Registration of the reference oracle as a backend."
responsibility = "Adapt the reference interpreter to the backend contract for deterministic conformance execution."

[[crate]]
package = "vyre-driver-spirv"
path = "vyre-driver-spirv"
layer = "concrete-backend"
seam = "spirv-driver"
interface = "SPIR-V backend registration, capability probing and dispatch."
responsibility = "Own SPIR-V target compilation, immutable module-bundle emission, Vulkan materialization and dispatch integration, and backend evidence."

[[crate]]
package = "vyre-driver-wgpu"
path = "vyre-driver-wgpu"
layer = "concrete-backend"
seam = "portable-driver"
interface = "Portable backend registration, capability probing and dispatch."
responsibility = "Own pure WGSL target compilation, portable GPU acquisition, materialization, dispatch, graph execution, and backend evidence."
facade_exported = true

[[crate]]
package = "vyre-emit-metal"
path = "vyre-emit-metal"
layer = "emitter"
seam = "metal-emitter"
interface = "Native Apple source emission for one selected module."
responsibility = "Consume verified lowering products and emit native Apple shader source through the shared emitter path."

[[crate]]
package = "vyre-emit-naga"
path = "vyre-emit-naga"
layer = "emitter"
seam = "primary-text-emitter"
interface = "Primary text emission and the related binary form for one selected module."
responsibility = "Consume verified lowering products and emit the primary text representation and related binary targets."

[[crate]]
package = "vyre-emit-ptx"
path = "vyre-emit-ptx"
layer = "emitter"
seam = "primary-binary-emitter"
interface = "Primary binary text emission for one selected module."
responsibility = "Consume verified lowering products and emit the primary binary backend text artifact."

[[crate]]
package = "vyre-emit-spirv"
path = "vyre-emit-spirv"
layer = "emitter"
seam = "spirv-emitter"
interface = "SPIR-V emission for one selected module."
responsibility = "Consume verified lowering products and emit SPIR-V artifacts through the shared writer."

[[crate]]
package = "vyre-foundation"
path = "vyre-foundation"
layer = "foundation"
seam = "foundation-ir"
interface = "Typed IR, graph, diagnostics, validation and semantic optimization contracts."
responsibility = "Own validated ProgramGraph, versioned schedule-free LogicalProgramGraph domains, versioned backend-neutral schedule IR and transform legality, semantic identity, neutral schedule-constraint composition, diagnostics, serialization, semantic operation registration, and backend-neutral optimization."
facade_exported = true

[[crate]]
package = "vyre-libs"
path = "vyre-libs"
layer = "libraries"
seam = "semantic-library"
interface = "Every composition in the workspace, re-exported from the domain crates behind one feature set. Returns Programs."
responsibility = "Own every composition in the workspace: consumer dialects and compiler-internal solvers, encoding, analysis, scheduling, and reasoning. Returns Programs. No backend, no emitter, no host rewrite of IR."

[[crate]]
package = "vyre-libs-analysis"
path = "vyre-libs-analysis"
layer = "libraries"
seam = "libs-analysis"
interface = "Static analysis, cost model, dataflow fixpoint and diagnostic compositions."
responsibility = "Own compiler-internal static analysis, cost models, dataflow fixpoint routines, and diagnostic aggregation. Does not own pass engine scheduling, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-bitset"
path = "vyre-libs-bitset"
layer = "libraries"
seam = "libs-bitset"
interface = "Packed bitset word operations and logical bitwise compositions."
responsibility = "Own packed u32 bitset operations, word utilities, and logical bitwise IR compositions. Does not own reduction tree implementations, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-builder"
path = "vyre-libs-builder"
layer = "libraries"
seam = "libs-builder"
interface = "Shared IR composition infrastructure, child region skeletons, operands and registration link anchors."
responsibility = "Own shared IR composition infrastructure, child region skeletons, operand wrappers, and registration link anchors for library builders. Does not own concrete domain operations, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-decode"
path = "vyre-libs-decode"
layer = "libraries"
seam = "libs-decode"
interface = "Base64, hex, DEFLATE and encodex decoding compositions."
responsibility = "Own Base64, hex, DEFLATE, and encodex data decoding and decompression IR compositions. Does not own parsing drivers, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-device"
path = "vyre-libs-device"
layer = "libraries"
seam = "libs-device"
interface = "Device boundary contracts, memory ownership and resident graph layout compositions."
responsibility = "Own compiler-internal device boundary contracts, memory ownership models, and resident graph layout compositions. Does not own concrete backend implementations or driver dispatch."

[[crate]]
package = "vyre-libs-encoding"
path = "vyre-libs-encoding"
layer = "libraries"
seam = "libs-encoding"
interface = "Provenance, matroid and fingerprint encoding compositions."
responsibility = "Own compiler-internal bitset, provenance, matroid, and fingerprint encoding compositions. Does not own consumer decoding algorithms, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-fixpoint"
path = "vyre-libs-fixpoint"
layer = "libraries"
seam = "libs-fixpoint"
interface = "Deterministic fixpoint iteration and grid synchronization compositions."
responsibility = "Own deterministic fixpoint iteration kernels, routing contracts, and grid synchronization barriers. Does not own domain dataflow analysis, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-graph"
path = "vyre-libs-graph"
layer = "libraries"
seam = "libs-graph"
interface = "Graph algorithm compositions: CSR traversal, AST walks, dominator trees and topological sort."
responsibility = "Own graph algorithms, CSR traversal, AST walks, dominator trees, and topological sorting IR compositions. Does not own pattern matching engines, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-hash"
path = "vyre-libs-hash"
layer = "libraries"
seam = "libs-hash"
interface = "Hash and checksum compositions."
responsibility = "Own hash and checksum compositions including FNV-1a, CRC-32, Adler-32, and BLAKE3 IR builders. Does not own security taint logic, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-math"
path = "vyre-libs-math"
layer = "libraries"
seam = "libs-math"
interface = "Linear algebra, matrix, scan, broadcasting, algebra and succinct structure compositions."
responsibility = "Own linear algebra, matrix operations, broadcasting, scans, optimization builders, and geometric algebra IR compositions. Does not own neural network layers, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-nn"
path = "vyre-libs-nn"
layer = "libraries"
seam = "libs-nn"
interface = "Neural network activation, linear, normalization, attention and mixture-of-experts compositions."
responsibility = "Own neural network activations, linear transformations, normalization, attention, mixture-of-experts, and language model inference IR compositions. Does not own general matrix kernels, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-parsing"
path = "vyre-libs-parsing"
layer = "libraries"
seam = "libs-parsing"
interface = "Lexer driver, LR(1) table walker and AST construction compositions."
responsibility = "Own lexer drivers, LR(1) table walkers, and language-specific AST construction IR compositions. Does not own semantic analysis passes, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-pattern"
path = "vyre-libs-pattern"
layer = "libraries"
seam = "libs-pattern"
interface = "Substring, DFA, NFA, regular expression and bracket matching compositions."
responsibility = "Own substring matching, DFA, NFA, bracket matching, and regular expression scanning pipeline IR compositions. Does not own token lexing or parser tables, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-reasoning"
path = "vyre-libs-reasoning"
layer = "libraries"
seam = "libs-reasoning"
interface = "Logic, causal reasoning and knowledge compilation compositions."
responsibility = "Own compiler-internal logic, causal reasoning, categorical rewrites, and knowledge compilation compositions. Does not own cost model evaluation, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-reduce"
path = "vyre-libs-reduce"
layer = "libraries"
seam = "libs-reduce"
interface = "Workgroup reduction tree, atomic scalar reduction and prefix scan compositions."
responsibility = "Own workgroup reduction trees, atomic scalar reductions, and prefix scan IR compositions. Does not own domain-specific algorithms, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-rule"
path = "vyre-libs-rule"
layer = "libraries"
seam = "libs-rule"
interface = "Detection rule condition operator and formula compositions."
responsibility = "Own detection rule engine condition operators, formulas, and program evaluation builders. Does not own security predicate solvers, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-scheduling"
path = "vyre-libs-scheduling"
layer = "libraries"
seam = "libs-scheduling"
interface = "Scheduling, fusion and batching compositions."
responsibility = "Own compiler-internal scheduling, fusion, batching, and dispatch strategy compositions. Does not own physical lowering, device execution, or runtime lifecycle policy."

[[crate]]
package = "vyre-libs-security"
path = "vyre-libs-security"
layer = "libraries"
seam = "libs-security"
interface = "Security taint analysis and label resolver compositions."
responsibility = "Own security taint analysis compositions, predicate evaluators, and label resolver IR builders. Does not own rule engine AST evaluation, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-solvers"
path = "vyre-libs-solvers"
layer = "libraries"
seam = "libs-solvers"
interface = "Numerical solver and autotuning compositions."
responsibility = "Own compiler-internal numerical solvers, autotuning routines, and spectral schedule analysis compositions. Does not own schedule search ranking, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-text"
path = "vyre-libs-text"
layer = "libraries"
seam = "libs-text"
interface = "Text classification, UTF-8 validation and line indexing compositions."
responsibility = "Own text processing, byte classification, UTF-8 validation, and line indexing IR compositions. Does not own decoding decompression algorithms, backend lowering, or runtime execution."

[[crate]]
package = "vyre-libs-vfs"
path = "vyre-libs-vfs"
layer = "libraries"
seam = "libs-vfs"
interface = "Virtual filesystem asynchronous block load compositions."
responsibility = "Own virtual filesystem DMA asynchronous block load and asset resolution compositions. Does not own host file IO or runtime storage drivers."

[[crate]]
package = "vyre-libs-visual"
path = "vyre-libs-visual"
layer = "libraries"
seam = "libs-visual"
interface = "Visual rendering and compositing effect compositions."
responsibility = "Own visual rendering and compositing effects including blur, shadow, blend modes, gradients, and color conversions. Does not own display presentation, backend lowering, or runtime execution."

[[crate]]
package = "vyre-lints"
path = "vyre-lints"
layer = "tooling"
seam = "lint-policy"
interface = "Workspace lint policy as a checkable declaration."
responsibility = "Enforce source-level project policies without depending on runtime crates."

[[crate]]
package = "vyre-lower"
path = "vyre-lower"
layer = "lowering"
seam = "lowering"
interface = "The single verified selected-module representation, lowered from semantic IR."
responsibility = "Consume validated selected schedule phases and semantic programs, then own the single validated PhysicalKernel lowering boundary plus its backend-neutral pre-emission transforms."

[[crate]]
package = "vyre-macros"
path = "vyre-macros"
layer = "foundation"
seam = "registration-macros"
interface = "Compile-time registration generation."
responsibility = "Provide compile-time registration and declaration macros without depending on runtime crates."

[[crate]]
package = "vyre-megakernel"
path = "vyre-megakernel"
layer = "compiler-boundary"
seam = "megakernel-compiler"
interface = "Schedule search, immutable artifact identity and authenticated target-payload construction."
responsibility = "Construct foundation-owned selected schedules through bounded whole-ProgramGraph search, and own immutable Artifact identity and authenticated TargetPayload construction. Does not own logical semantics, schedule schemas, physical-kernel lowering, admission, execution, or lifecycle policy."
facade_exported = true

[[crate]]
package = "vyre-pass-engine"
path = "vyre-pass-engine"
layer = "pass-engine"
seam = "pass-engine"
interface = "Optimizer pass execution as dispatched Programs."
responsibility = "Execute optimizer passes as Vyre Programs through compiler-owned semantic compilation and admitted artifact submission."

[[crate]]
package = "vyre-primitives"
path = "vyre-primitives"
layer = "primitives"
seam = "primitive-library"
interface = "Intrinsic operations, the wire format, guarded IR construction, launch-geometry helpers and marker types."
responsibility = "Own marker types and uncomposable hardware intrinsics. A composition belongs in vyre-libs, not here."

[[crate]]
package = "vyre-reference"
path = "vyre-reference"
layer = "semantics"
seam = "reference-semantics"
interface = "Independent semantic oracle evaluation and canonical ULP distance."
responsibility = "The only crate permitted to compute on the CPU: the pure-Rust IR oracle. Not a backend and not a fallback."

[[crate]]
package = "vyre-registry-link"
path = "vyre-registry-link"
layer = "registry-link"
seam = "registry-link"
interface = "Linked inventory registry sources and the per-source floor."
responsibility = "Own every inventory registry link anchor, report which sources a build links, and assert that each linked source reached the registry it submits into."

[[crate]]
package = "vyre-runtime"
path = "vyre-runtime"
layer = "runtime"
seam = "runtime"
interface = "Artifact admission, residency, submission, recovery and readback lifecycle."
responsibility = "Execute the artifact's selected persistence: sessions, recovery, residency, scheduling, caches, telemetry, readback, and IO. Does not decide whether to be persistent."
facade_exported = true

[[crate]]
package = "vyre-safetensors"
path = "vyre-safetensors"
layer = "runtime"
seam = "safetensors-adapter"
interface = "Container-format mapping into typed transfer descriptors."
responsibility = "Validate safetensors metadata, shard indexes, compiler requirements, trusted shard digests, and immutable checkpoint identities without owning runtime residency."

[[crate]]
package = "vyre-spec"
path = "vyre-spec"
layer = "foundation"
seam = "specification"
interface = "Stable cross-engine schemas, declared operation signatures and IR levels."
responsibility = "Own stable schemas, operation definitions, and compatibility contracts without runtime dependencies."
facade_exported = true

[[crate]]
package = "vyre-test-support"
path = "vyre-test-support"
layer = "test-tooling"
seam = "test-support"
interface = "Shared verification fixtures and harness support."
responsibility = "Provide shared deterministic fixtures and assertions for workspace tests."

[[crate]]
package = "xtask"
path = "xtask"
layer = "tooling"
seam = "gate-registry"
interface = "The subcommand registry and every gate that judges the tree from source text, manifests, workflows and recorded evidence."
responsibility = "Own the subcommand registry and every gate that judges the tree from source text, manifests, workflows, and recorded evidence, linking no vyre crate."

[[crate]]
package = "xtask-evidence"
path = "xtask-evidence"
layer = "tooling"
seam = "evidence-gates"
interface = "The gates that decide whether a recorded benchmark or release measurement still describes this tree."
responsibility = "Own the xtask subcommands that decide whether a recorded benchmark or release measurement still describes this tree."

# The consumer facade roster.
#
# `vyre-libs` is the one curated consumer facade over the domain packages of
# the `libraries` layer. Its `[features]` table is generated from the rows
# below by `cargo xtask configuration-model --write`, so a domain feature is
# recorded once, beside the `[[crate]]` record of the package that owns its
# semantics, instead of twice in two manifests that can disagree.
#
# A `[[facade.feature]]` row states which package owns the capability. The
# generated manifest row forwards to that package and to nothing else, so the
# facade is never a second declaring package for the name. `requires` names
# the facade features the row also selects, which is the facade's own curation
# of what a selection exposes and is not derivable from the domain manifest.
# `activates` names third-party optional dependencies the capability needs.

[facade]
package = "vyre-libs"
default = [
    "math-linalg",
    "math-scan",
    "math-broadcast",
    "nn-activation",
    "nn-linear",
    "nn-norm",
    "pattern-substring",
    "pattern-dfa",
    "hash",
    "decode",
]
local = ["test-fixtures"]

# A named bundle of facade features. An aggregate forwards to no package of
# its own, so it declares nothing and owns nothing.

[[facade.aggregate]]
name = "math"
selects = ["math-linalg", "math-scan", "math-broadcast", "math-algebra", "math-succinct"]

[[facade.aggregate]]
name = "nn"
selects = ["nn-activation", "nn-linear", "nn-norm", "nn-attention"]

[[facade.aggregate]]
name = "pattern"
selects = ["pattern-substring", "pattern-dfa"]

[[facade.aggregate]]
name = "crypto"
selects = ["crypto-blake3"]

# One row per domain package the facade forwards to. `optional` states
# whether the facade declares the dependency optional, which decides whether a
# generated row activates it with `dep:` and forwards weakly.

[[facade.domain]]
package = "vyre-libs-builder"
optional = false

[[facade.domain]]
package = "vyre-libs-bitset"
optional = true

[[facade.domain]]
package = "vyre-libs-reduce"
optional = true

[[facade.domain]]
package = "vyre-libs-fixpoint"
optional = true

[[facade.domain]]
package = "vyre-libs-math"
optional = true

[[facade.domain]]
package = "vyre-libs-nn"
optional = true

[[facade.domain]]
package = "vyre-libs-graph"
optional = true

[[facade.domain]]
package = "vyre-libs-pattern"
optional = true

[[facade.domain]]
package = "vyre-libs-hash"
optional = true

[[facade.domain]]
package = "vyre-libs-text"
optional = true

[[facade.domain]]
package = "vyre-libs-decode"
optional = true

[[facade.domain]]
package = "vyre-libs-parsing"
optional = true

[[facade.domain]]
package = "vyre-libs-security"
optional = true

[[facade.domain]]
package = "vyre-libs-visual"
optional = true

[[facade.domain]]
package = "vyre-libs-rule"
optional = true

[[facade.domain]]
package = "vyre-libs-vfs"
optional = true

[[facade.domain]]
package = "vyre-libs-device"
optional = true

[[facade.domain]]
package = "vyre-libs-solvers"
optional = true

[[facade.domain]]
package = "vyre-libs-encoding"
optional = true

[[facade.domain]]
package = "vyre-libs-analysis"
optional = true

[[facade.domain]]
package = "vyre-libs-reasoning"
optional = true

[[facade.domain]]
package = "vyre-libs-scheduling"
optional = true

# One row per domain feature the facade exposes.

[[facade.feature]]
name = "builder"
domain = "vyre-libs-builder"
requires = []

[[facade.feature]]
name = "builder-ops"
domain = "vyre-libs-builder"
requires = ["builder"]

[[facade.feature]]
name = "cat-a-builder-options"
domain = "vyre-libs-builder"
requires = ["builder"]

[[facade.feature]]
name = "telemetry"
domain = "vyre-libs-builder"
requires = []

[[facade.feature]]
name = "bitset"
domain = "vyre-libs-bitset"
requires = ["reduce"]

[[facade.feature]]
name = "logical"
domain = "vyre-libs-bitset"
requires = ["bitset"]

[[facade.feature]]
name = "reduce"
domain = "vyre-libs-reduce"
requires = ["builder"]

[[facade.feature]]
name = "fixpoint"
domain = "vyre-libs-fixpoint"
requires = ["bitset"]

[[facade.feature]]
name = "math-dialect"
domain = "vyre-libs-math"
requires = ["builder-ops", "reduce"]

[[facade.feature]]
name = "math-kernels"
domain = "vyre-libs-math"
requires = ["reduce"]

[[facade.feature]]
name = "math-linalg"
domain = "vyre-libs-math"
requires = ["builder-ops", "cat-a-builder-options", "math-dialect", "math-kernels"]

[[facade.feature]]
name = "math-scan"
domain = "vyre-libs-math"
requires = ["math-dialect", "math-kernels"]

[[facade.feature]]
name = "math-broadcast"
domain = "vyre-libs-math"
requires = ["math-dialect", "math-kernels"]

[[facade.feature]]
name = "math-algebra"
domain = "vyre-libs-math"
requires = ["math-dialect", "math-kernels"]

[[facade.feature]]
name = "math-succinct"
domain = "vyre-libs-math"
requires = ["math-dialect", "math-kernels", "graph"]

[[facade.feature]]
name = "geom"
domain = "vyre-libs-math"
requires = ["math-kernels"]

[[facade.feature]]
name = "opt"
domain = "vyre-libs-math"
requires = ["math-kernels"]

[[facade.feature]]
name = "representation"
domain = "vyre-libs-math"
requires = ["builder"]

[[facade.feature]]
name = "nn-kernels"
domain = "vyre-libs-nn"
requires = ["math-kernels"]

[[facade.feature]]
name = "nn-activation"
domain = "vyre-libs-nn"
requires = ["builder-ops", "nn-kernels"]

[[facade.feature]]
name = "nn-linear"
domain = "vyre-libs-nn"
requires = ["cat-a-builder-options", "math-linalg", "nn-activation", "nn-kernels"]

[[facade.feature]]
name = "nn-linear-4bit"
domain = "vyre-libs-nn"
requires = ["nn-linear"]

[[facade.feature]]
name = "nn-norm"
domain = "vyre-libs-nn"
requires = ["builder-ops", "cat-a-builder-options", "math-broadcast", "nn-kernels"]

[[facade.feature]]
name = "nn-attention"
domain = "vyre-libs-nn"
requires = ["builder-ops", "cat-a-builder-options", "nn-norm", "math-linalg", "nn-kernels"]

[[facade.feature]]
name = "nn-moe"
domain = "vyre-libs-nn"
requires = ["nn-attention"]

[[facade.feature]]
name = "nn-inference"
domain = "vyre-libs-nn"
requires = ["nn-activation", "nn-linear", "nn-norm", "nn-attention", "nn-moe"]

[[facade.feature]]
name = "llm"
domain = "vyre-libs-nn"
requires = ["builder-ops", "nn-moe"]

[[facade.feature]]
name = "graph"
domain = "vyre-libs-graph"
requires = ["bitset", "reduce", "hash", "fixpoint", "math-kernels", "telemetry"]

[[facade.feature]]
name = "topology"
domain = "vyre-libs-graph"
requires = ["math-kernels", "graph"]

[[facade.feature]]
name = "graph-dispatch"
domain = "vyre-libs-graph"
requires = ["device", "telemetry", "math-kernels", "graph"]

[[facade.feature]]
name = "interactive-graphics"
domain = "vyre-libs-graph"
requires = ["graph", "visual"]

[[facade.feature]]
name = "pattern-kernels"
domain = "vyre-libs-pattern"
requires = ["hash"]

[[facade.feature]]
name = "pattern-substring"
domain = "vyre-libs-pattern"
requires = ["pattern-dfa", "pattern-kernels"]

[[facade.feature]]
name = "pattern-dfa"
domain = "vyre-libs-pattern"
requires = ["pattern-kernels", "text"]

[[facade.feature]]
name = "pattern-nfa"
domain = "vyre-libs-pattern"
requires = ["pattern-dfa", "nfa"]

[[facade.feature]]
name = "pattern-regex"
domain = "vyre-libs-pattern"
requires = ["pattern-nfa"]

[[facade.feature]]
name = "nfa"
domain = "vyre-libs-pattern"
requires = ["bitset", "pattern-kernels"]

[[facade.feature]]
name = "hash"
domain = "vyre-libs-hash"
requires = ["builder"]

[[facade.feature]]
name = "crypto-blake3"
domain = "vyre-libs-hash"
requires = ["hash"]

[[facade.feature]]
name = "text"
domain = "vyre-libs-text"
requires = ["reduce"]

[[facade.feature]]
name = "decode"
domain = "vyre-libs-decode"
requires = ["pattern-kernels", "text"]

[[facade.feature]]
name = "parsing"
domain = "vyre-libs-parsing"
requires = ["go-parser", "python-parser"]

[[facade.feature]]
name = "parsing-kernels"
domain = "vyre-libs-parsing"
requires = ["text", "pattern-kernels"]

[[facade.feature]]
name = "go-parser"
domain = "vyre-libs-parsing"
requires = ["reduce"]

[[facade.feature]]
name = "python-parser"
domain = "vyre-libs-parsing"
requires = ["parsing-kernels"]

[[facade.feature]]
name = "security"
domain = "vyre-libs-security"
requires = ["label", "predicate"]

[[facade.feature]]
name = "predicate"
domain = "vyre-libs-security"
requires = ["graph", "bitset", "label"]

[[facade.feature]]
name = "label"
domain = "vyre-libs-security"
requires = ["bitset"]

[[facade.feature]]
name = "visual"
domain = "vyre-libs-visual"
requires = ["math-kernels"]

[[facade.feature]]
name = "rule"
domain = "vyre-libs-rule"
requires = ["builder"]

[[facade.feature]]
name = "vfs"
domain = "vyre-libs-vfs"
requires = []

[[facade.feature]]
name = "device"
domain = "vyre-libs-device"
requires = []

[[facade.feature]]
name = "solvers"
domain = "vyre-libs-solvers"
requires = ["device", "telemetry", "math-kernels", "topology"]

[[facade.feature]]
name = "encoding"
domain = "vyre-libs-encoding"
requires = ["device", "telemetry", "pattern-kernels", "math-kernels", "nn-kernels", "parsing-kernels", "bitset", "graph", "fixpoint"]

[[facade.feature]]
name = "analysis"
domain = "vyre-libs-analysis"
requires = ["device", "telemetry", "math-kernels", "graph"]

[[facade.feature]]
name = "reasoning"
domain = "vyre-libs-reasoning"
requires = ["analysis", "graph"]

[[facade.feature]]
name = "scheduling"
domain = "vyre-libs-scheduling"
requires = ["device", "telemetry", "math-kernels", "parsing-kernels", "graph"]

# Declared configuration constraints. `configuration-model` proves these over
# every cell a build can ask for, and reports a constraint that names a feature
# no package declares, because such a constraint can never bind while reading
# as one.

# A feature name every package spells the same way on purpose. The
# one-owning-package rule does not apply, because the capability is a
# per-package convention rather than one capability many packages expose.

[[configuration.shared_capability]]
name = "cpu-parity"
reason = "each crate enables its own comparison against vyre-reference, so the switch is per-crate by construction"

[[configuration.shared_capability]]
name = "device-tests"
reason = "each crate enables its own tests that require a real device, and the sets do not overlap"

[[configuration.shared_capability]]
name = "subgroup-ops"
reason = "each crate declares the subgroup capability its own emitters or lowering paths need"

[[configuration.shared_capability]]
name = "test-fixtures"
reason = "each crate publishes its own fixture module for downstream test code"

[[configuration.shared_capability]]
name = "cuda"
reason = "vyre, vyre-registry-link and vyre-driver-cuda each gate their own linkage of the CUDA driver, so the switch is per-crate"

[[configuration.shared_capability]]
name = "wgpu"
reason = "vyre, vyre-registry-link and vyre-driver-wgpu each gate their own linkage of the wgpu driver, so the switch is per-crate"

[[configuration.shared_capability]]
name = "gpu"
reason = "vyre-conform and vyre-primitives each gate their own requirement for a real device, and the sets do not overlap"

[[configuration.shared_capability]]
name = "libs-compositions"
reason = "vyre-driver and vyre-runtime each select the vyre-libs domains their own compositions need, and neither set is the other"
[[crate]]
package = "xtask-registry"
path = "xtask-registry"
layer = "tooling"
seam = "live-registry-gates"
interface = "The gates that must observe the live operation registry, the primitive catalog or a linked backend driver."
responsibility = "Own the xtask subcommands that must observe the live operation registry, the primitive catalog behind it, or a linked backend driver."
