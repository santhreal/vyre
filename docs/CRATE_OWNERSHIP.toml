schema_version = 3

# The layer DAG, and the whole of the direction contract.
#
# `rank` is dependency depth: the longest chain of cross-layer production edges
# from a layer down to one that depends on nothing outside itself. A production
# edge is legal when the source rank is strictly greater than the destination
# rank, and same-layer edges are always legal. That single comparison rejects a
# reversal and makes a layer cycle unrepresentable, so there is no roster of
# permitted layer pairs to keep in agreement with the manifests.
#
# Two layers share a rank when neither depends on the other. A new edge between
# two such layers fails the direction rule, which is the decision worth
# stopping on: one of the two ranks is then wrong and the manifest records
# which.

[[layer]]
name = "foundation"
rank = 0
purpose = "Typed IR, logical domains, specification records, diagnostics, and the derive macros that generate them."

[[layer]]
name = "standalone-tooling"
rank = 0
purpose = "Source-structure gates that read the tree and depend on no compiler crate."

[[layer]]
name = "lowering"
rank = 1
purpose = "One verified selected-module representation between semantic IR and target emission."

[[layer]]
name = "primitives"
rank = 1
purpose = "Intrinsic operations, each with its own emitter arm in every backend and its own reference arm."

[[layer]]
name = "compiler-boundary"
rank = 2
purpose = "Schedule search, artifact identity, and authenticated target-payload construction."

[[layer]]
name = "emitter"
rank = 2
purpose = "Target text and binary emission for one dialect each."

[[layer]]
name = "semantics"
rank = 2
purpose = "The independent semantic oracle. Never a production execution route."

[[layer]]
name = "libraries"
rank = 3
purpose = "Domain-neutral compositions built from existing IR."

[[layer]]
name = "test-tooling"
rank = 5
purpose = "Shared verification fixtures and harness support, private to the workspace."

[[layer]]
name = "backend-neutral"
rank = 4
purpose = "The dispatch, binding, and capability contract every concrete backend implements."

[[layer]]
name = "pass-engine"
rank = 4
purpose = "Pass scheduling and rewrite application over semantic IR."

[[layer]]
name = "concrete-backend"
rank = 5
purpose = "One device family each: capability probing, module loading, and dispatch."

[[layer]]
name = "packaging"
rank = 5
purpose = "Ahead-of-time artifact packaging and load."

[[layer]]
name = "runtime"
rank = 5
purpose = "Submission, admission, tenancy, and lifecycle policy over a compiled artifact."

[[layer]]
name = "facade"
rank = 6
purpose = "The single curated consumer surface over the layers below it."

[[layer]]
name = "registry-link"
rank = 6
purpose = "Link anchors that pull registration submissions into a final binary."

[[layer]]
name = "conformance"
rank = 7
purpose = "Parity harnesses that join a production result to an independently produced oracle result."

[[layer]]
name = "tooling"
rank = 7
purpose = "Gates, benchmarks, and generators that read the whole workspace."

[[crate]]
package = "vyre-megakernel"
publication_class = "internal-engine"
path = "vyre-megakernel"
owner = "megakernel-compiler"
layer = "compiler-boundary"
responsibility = "Construct foundation-owned selected schedules through bounded whole-ProgramGraph search, and own immutable Artifact identity and authenticated TargetPayload construction. Does not own logical semantics, schedule schemas, physical-kernel lowering, admission, execution, or lifecycle policy."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed graph, logical-domain, neutral schedule IR and validation, diagnostics, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "single verified selected-module representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-spec"
purpose = "the declared IR level the target-payload stage registers itself for"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "structure-gate"
publication_class = "conformance-tooling"
path = "structure-gate"
owner = "release-tooling"
layer = "standalone-tooling"
responsibility = "Enforce the crate roster, one operation identity per semantic operation, one home per concept, and one place per module. Depends on no vyre crate so it keeps running while the workspace does not compile."

[[crate]]
package = "vyre"
publication_class = "stable-consumer-sdk"
path = "vyre"
owner = "public-facade"
layer = "facade"
responsibility = "Public facade. Re-export IR, driver, runtime, and the artifact compiler. Own no logic."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-foundation"
publication_class = "extension-sdk"
path = "vyre-foundation"
owner = "foundation-ir"
layer = "foundation"
responsibility = "Own validated ProgramGraph, versioned schedule-free LogicalProgramGraph domains, versioned backend-neutral schedule IR and transform legality, semantic identity, neutral schedule-constraint composition, diagnostics, serialization, semantic operation registration, and backend-neutral optimization."

[[crate.dependency]]
package = "vyre-macros"
purpose = "compile-time registration generation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "registration-macros"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver"
publication_class = "extension-sdk"
path = "vyre-driver"
owner = "backend-contract"
layer = "backend-neutral"
responsibility = "Define backend-neutral device, target compiler registration, artifact materialization, binding, submission, completion, capability, dispatch, and evidence contracts."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-libs"
purpose = "composition library the driver adapters plan against"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver-metal"
publication_class = "concrete-backend"
path = "vyre-driver-metal"
owner = "metal-driver"
layer = "concrete-backend"
responsibility = "Own pure MSL target compilation, native Apple device acquisition, materialization, dispatch, and backend evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-metal"
purpose = "native Apple source emission"
features = []
conditions = ["cfg(any(target_os = \"macos\", target_os = \"ios\"))"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "metal-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate]]
package = "vyre-driver-wgpu"
publication_class = "concrete-backend"
path = "vyre-driver-wgpu"
owner = "portable-driver"
layer = "concrete-backend"
responsibility = "Own pure WGSL target compilation, portable GPU acquisition, materialization, dispatch, graph execution, and backend evidence."

[[crate.dependency]]
package = "vyre-libs"
purpose = "composition trees the portable adapters plan against"
features = ["reasoning"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = ["libs-compositions"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-pass-engine"
purpose = "optimizer pass execution as dispatched Vyre Programs"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "pass-engine"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver-spirv"
publication_class = "concrete-backend"
path = "vyre-driver-spirv"
owner = "spirv-driver"
layer = "concrete-backend"
responsibility = "Own SPIR-V target compilation, immutable module-bundle emission, Vulkan materialization and dispatch integration, and backend evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-spirv"
purpose = "SPIR-V emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "spirv-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate]]
package = "vyre-driver-cuda"
publication_class = "concrete-backend"
path = "vyre-driver-cuda"
owner = "cuda-driver"
layer = "concrete-backend"
responsibility = "Own pure PTX target compilation, native device acquisition, materialization, dispatch, graphs, and release-path evidence."

[[crate.dependency]]
package = "vyre-libs"
purpose = "composition trees the CUDA adapters plan against"
features = ["device", "graph-dispatch", "scheduling"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-ptx"
purpose = "primary binary backend text emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-binary-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-pass-engine"
purpose = "optimizer pass execution as dispatched Vyre Programs"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "pass-engine"

[[crate]]
package = "vyre-driver-reference"
publication_class = "concrete-backend"
path = "vyre-driver-reference"
owner = "reference-driver"
layer = "concrete-backend"
responsibility = "Adapt the reference interpreter to the backend contract for deterministic conformance execution."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "the admitted artifact and target payload the reference target materializes"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate]]
package = "vyre-registry-link"
publication_class = "internal-engine"
path = "vyre-registry-link"
owner = "registry-link"
layer = "registry-link"
responsibility = "Own every inventory registry link anchor, report which sources a build links, and assert that each linked source reached the registry it submits into."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend registry contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "operation registry contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "the physical-kernel level-stage registry source"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "the target-payload level-stage registry source"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend registration"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-metal"
purpose = "native Apple backend registration"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "metal-driver"

[[crate.dependency]]
package = "vyre-driver-spirv"
purpose = "SPIR-V backend registration"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "spirv-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend registration"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation registrations"
features = ["full"]
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "primitive operation registrations"
features = ["hardware"]
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate]]
package = "vyre-reference"
publication_class = "internal-engine"
path = "vyre-reference"
owner = "reference-semantics"
layer = "semantics"
responsibility = "The only crate permitted to compute on the CPU: the pure-Rust IR oracle. Not a backend and not a fallback."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"


[[crate.dependency]]
package = "vyre-primitives"
purpose = "the wire format, the marker types, and guarded IR construction"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-spec"
publication_class = "extension-sdk"
path = "vyre-spec"
owner = "specification"
layer = "foundation"
responsibility = "Own stable schemas, operation definitions, and compatibility contracts without runtime dependencies."

[[crate]]
package = "vyre-macros"
publication_class = "extension-sdk"
path = "vyre-macros"
owner = "registration-macros"
layer = "foundation"
responsibility = "Provide compile-time registration and declaration macros without depending on runtime crates."

[[crate]]
package = "vyre-primitives"
publication_class = "extension-sdk"
path = "vyre-primitives"
owner = "primitive-library"
layer = "primitives"
responsibility = "Own marker types and uncomposable hardware intrinsics. A composition belongs in vyre-libs, not here."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate]]
package = "vyre-test-support"
publication_class = "private-test-support"
path = "vyre-test-support"
owner = "test-support"
layer = "test-tooling"
responsibility = "Provide shared deterministic fixtures and assertions for workspace tests."

[[crate.dependency]]
package = "structure-gate"
purpose = "resolve the checkout a gate reports on from the working directory at run time"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"
[[crate.dependency]]
package = "vyre-driver"
purpose = "the backend-neutral driver registry contract every backend fixture is stated against, behind the driver-contracts feature"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "backend-contract"
[[crate.dependency]]
package = "vyre-foundation"
purpose = "IR statement fixtures for the run-time variant enumeration, behind the ir-fixtures feature"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "foundation-ir"
[[crate.dependency]]
package = "vyre-megakernel"
purpose = "the semantic execution request every backend contract shares, behind the semantic-requests feature"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "megakernel-compiler"
[[crate.dependency]]
package = "vyre-reference"
purpose = "reference interpreter oracle evaluation and canonical ULP distance calculation for the differential execution matrix, behind the ir-fixtures feature"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "reference-semantics"
[[crate.dependency]]
package = "vyre-spec"
purpose = "DataType and declared operation signatures for fixture tables, without gating a leaf crate behind ir-fixtures"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-pass-engine"
publication_class = "internal-engine"
path = "vyre-pass-engine"
owner = "pass-engine"
layer = "pass-engine"
responsibility = "Execute optimizer passes as Vyre Programs through compiler-owned semantic compilation and admitted artifact submission."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate]]
package = "vyre-conform-spec"
publication_class = "conformance-tooling"
path = "conform/vyre-conform-spec"
owner = "conformance"
layer = "conformance"
responsibility = "Define conformance case, result, and certificate schemas against the public facade."

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-conform"
publication_class = "conformance-tooling"
path = "conform/vyre-conform"
owner = "conformance"
layer = "conformance"
responsibility = "Execute production artifacts against independent reference semantics, minimize counterexamples, check algebraic laws, and issue versioned certificates and replay records through one library and thin CLI."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-conform-spec"
purpose = "versioned conformance schemas"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "conformance"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-spirv"
purpose = "SPIR-V backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "spirv-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["full"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["hardware"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate.dependency]]
package = "vyre-registry-link"
purpose = "linked inventory registry sources and the per-source floor"
features = ["operations"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "registry-link"

[[crate]]
package = "xtask"
publication_class = "conformance-tooling"
path = "xtask"
owner = "release-tooling"
layer = "tooling"
responsibility = "Own the subcommand registry and every gate that judges the tree from source text, manifests, workflows, and recorded evidence, linking no vyre crate."

[[crate.dependency]]
package = "structure-gate"
purpose = "resolve the checkout a gate reports on from the working directory at run time"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"

[[crate]]
package = "xtask-registry"
publication_class = "conformance-tooling"
path = "xtask-registry"
owner = "release-tooling"
layer = "tooling"
responsibility = "Own the xtask subcommands that must observe the live operation registry, the primitive catalog behind it, or a linked backend driver."

[[crate.dependency]]
package = "xtask"
purpose = "subcommand registry, bounded readers, and release manifests"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"

[[crate.dependency]]
package = "structure-gate"
purpose = "read whether a directory carries Rust source, and which directory owns a domain"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "neutral artifact compilation and target payload contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["full", "pattern-regex"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["hardware"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate.dependency]]
package = "vyre-registry-link"
purpose = "linked inventory registry sources and the per-source floor"
features = ["cuda", "operations", "spirv", "wgpu"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "registry-link"

[[crate]]
package = "xtask-evidence"
publication_class = "conformance-tooling"
path = "xtask-evidence"
owner = "release-tooling"
layer = "tooling"
responsibility = "Own the xtask subcommands that decide whether a recorded benchmark or release measurement still describes this tree."

[[crate.dependency]]
package = "xtask"
purpose = "subcommand registry, bounded readers, and release manifests"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"

[[crate.dependency]]
package = "vyre-bench"
purpose = "benchmark workloads and evidence"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "benchmarks"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "the release optimization family list the pass-family manifest is checked against"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-registry-link"
purpose = "linked inventory registry sources and the per-source floor"
features = ["cuda", "metal", "spirv", "wgpu"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "registry-link"

[[crate]]
package = "vyre-runtime"
publication_class = "internal-engine"
path = "vyre-runtime"
owner = "runtime"
layer = "runtime"
responsibility = "Execute the artifact's selected persistence: sessions, recovery, residency, scheduling, caches, telemetry, readback, and IO. Does not decide whether to be persistent."

[[crate.dependency]]
package = "vyre-libs"
purpose = "composition trees the megakernel planner plans against"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate]]
package = "vyre-safetensors"
publication_class = "stable-consumer-sdk"
path = "vyre-safetensors"
owner = "safetensors-adapter"
layer = "runtime"
responsibility = "Validate safetensors metadata, shard indexes, compiler requirements, trusted shard digests, and immutable checkpoint identities without owning runtime residency."

[[crate]]
package = "vyre-libs"
publication_class = "stable-consumer-sdk"
path = "vyre-libs"
owner = "product-libraries"
layer = "libraries"
responsibility = "Own every composition in the workspace: consumer dialects and compiler-internal solvers, encoding, analysis, scheduling, and reasoning. Returns Programs. No backend, no emitter, no host rewrite of IR."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = ["serde"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "the compiler-owned semantic compile-and-execute seam every composition reaches a device through"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "the wire format, guarded IR construction, the launch-geometry helper, the marker types, and the intrinsic registrations"
features = ["inventory-registry"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-aot"
publication_class = "internal-engine"
path = "vyre-aot"
owner = "aot-artifacts"
layer = "packaging"
responsibility = "Package the same megakernel artifact class ahead of time. Not a second compile path. No workspace crate currently depends on this one."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate]]
package = "vyre-alloc-probe"
publication_class = "conformance-tooling"
path = "vyre-alloc-probe"
owner = "benchmarks"
layer = "standalone-tooling"
responsibility = "Count heap traffic per thread behind a GlobalAlloc wrapper, so an allocation budget measures the code under test rather than the test schedule. Depends on no vyre crate, so a harness binary links it at run time and a driver suite links it as a dev-dependency."

[[crate]]
package = "vyre-bench"
publication_class = "conformance-tooling"
path = "vyre-bench"
owner = "benchmarks"
layer = "tooling"
responsibility = "Own reproducible workload benchmarks against the best available native baseline for each class, not against vyre's own unfused output."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-alloc-probe"
purpose = "per-thread heap counters the runner installs as the global allocator"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "benchmarks"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = ["test-fixtures"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["cfg(not(target_os = \"macos\"))"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-emit-ptx"
purpose = "primary binary backend text emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-binary-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["bitset", "graph", "math-scan", "nn-linear-4bit", "predicate"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-pass-engine"
purpose = "optimizer pass execution as dispatched Vyre Programs"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "pass-engine"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["hardware"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate.dependency]]
package = "vyre-registry-link"
purpose = "linked inventory registry sources and the per-source floor"
features = ["cuda", "metal", "spirv", "wgpu"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "registry-link"

[[crate.dependency]]
package = "xtask"
purpose = "the one producer of the source fingerprint a recorded artifact names its tree with"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "release-tooling"

[[crate]]
package = "vyre-lints"
publication_class = "conformance-tooling"
path = "vyre-lints"
owner = "lint-policy"
layer = "tooling"
responsibility = "Enforce source-level project policies without depending on runtime crates."

[[crate]]
package = "vyre-lower"
publication_class = "internal-engine"
path = "vyre-lower"
owner = "lowering"
layer = "lowering"
responsibility = "Consume validated selected schedule phases and semantic programs, then own the single validated PhysicalKernel lowering boundary plus its backend-neutral pre-emission transforms."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR plus validated backend-neutral selected schedule phases"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-spec"
purpose = "the declared IR level a lowering stage registers itself for"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-emit-naga"
publication_class = "internal-engine"
path = "vyre-emit-naga"
owner = "primary-text-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit the primary text representation and related binary targets."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts, and the source digest the build script stamps"
features = []
conditions = ["always"]
kinds = ["build", "normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-ptx"
publication_class = "internal-engine"
path = "vyre-emit-ptx"
owner = "primary-binary-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit the primary binary backend text artifact."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts, and the source digest the build script stamps"
features = []
conditions = ["always"]
kinds = ["build", "normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-spirv"
publication_class = "internal-engine"
path = "vyre-emit-spirv"
owner = "spirv-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit SPIR-V artifacts through the shared writer."

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "diagnostic projection for emitter errors"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-metal"
publication_class = "internal-engine"
path = "vyre-emit-metal"
owner = "metal-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit native Apple shader source through the shared emitter path."

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-debug"
publication_class = "internal-engine"
path = "vyre-debug"
owner = "debugging"
layer = "tooling"
responsibility = "Inspect, explain, and diagnose typed programs, lowering, and product-library composition."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["python-parser"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"
