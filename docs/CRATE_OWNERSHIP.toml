schema_version = 2

[[crate]]
package = "vyre-megakernel"
path = "vyre-megakernel"
owner = "megakernel-compiler"
layer = "compiler-boundary"
responsibility = "Explore and select legal whole-ProgramGraph fusion schedules under explicit SearchBudget bounds, expose selected modules and canonical ABI to registered target compilers, then emit canonical Artifact and ArtifactEnvelope records without owning admission, execution, or lifecycle policy."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "single verified selected-module representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate]]
package = "structure-gate"
path = "structure-gate"
owner = "release-tooling"
layer = "tooling"
responsibility = "Enforce the crate roster, one operation identity per semantic operation, and one home per concept. Depends on no vyre crate so it keeps running while the workspace does not compile."

[[crate]]
package = "vyre"
path = "vyre"
owner = "public-facade"
layer = "facade"
responsibility = "Expose canonical frontend IR, compiler artifact, runtime submission, and scan product entry points without re-owning backend contracts."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-foundation"
path = "vyre-foundation"
owner = "foundation-ir"
layer = "foundation"
responsibility = "Own typed IR and ProgramGraph contracts, validation, diagnostics, serialization, semantic operation registration, and backend-neutral optimization."

[[crate.dependency]]
package = "vyre-macros"
purpose = "compile-time registration generation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "registration-macros"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver"
path = "vyre-driver"
owner = "backend-contract"
layer = "backend-neutral"
responsibility = "Define backend-neutral device, target compiler registration, artifact materialization, binding, submission, completion, capability, dispatch, and evidence contracts."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-macros"
purpose = "compile-time registration generation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "registration-macros"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-self-substrate"
purpose = "optional scheduler and analysis substrate"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "self-substrate"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver-metal"
path = "vyre-driver-metal"
owner = "metal-driver"
layer = "concrete-backend"
responsibility = "Own pure MSL target compilation, native Apple device acquisition, materialization, dispatch, and backend evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-metal"
purpose = "native Apple source emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "metal-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate]]
package = "vyre-driver-wgpu"
path = "vyre-driver-wgpu"
owner = "portable-driver"
layer = "concrete-backend"
responsibility = "Own pure WGSL target compilation, portable GPU acquisition, materialization, dispatch, graph execution, and backend evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = ["self-substrate-adapters"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-self-substrate"
purpose = "optional scheduler and analysis substrate"
features = ["logic"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "self-substrate"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-driver-spirv"
path = "vyre-driver-spirv"
owner = "spirv-driver"
layer = "concrete-backend"
responsibility = "Own SPIR-V target compilation, immutable module-bundle emission, Vulkan materialization and dispatch integration, and backend evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-spirv"
purpose = "SPIR-V emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "spirv-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-driver-cuda"
path = "vyre-driver-cuda"
owner = "cuda-driver"
layer = "concrete-backend"
responsibility = "Own pure PTX target compilation, native device acquisition, materialization, dispatch, graphs, and release-path evidence."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-emit-ptx"
purpose = "primary binary backend text emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-binary-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-self-substrate"
purpose = "optional scheduler and analysis substrate"
features = ["graph-solvers", "scheduling"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "self-substrate"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-driver-reference"
path = "vyre-driver-reference"
owner = "reference-driver"
layer = "concrete-backend"
responsibility = "Adapt the reference interpreter to the backend contract for deterministic conformance execution."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate]]
package = "vyre-reference"
path = "vyre-reference"
owner = "reference-semantics"
layer = "semantics"
responsibility = "Execute programs with the canonical host oracle and produce semantic witnesses."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["hash", "matching"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-spec"
path = "vyre-spec"
owner = "specification"
layer = "foundation"
responsibility = "Own stable schemas, operation definitions, and compatibility contracts without runtime dependencies."

[[crate]]
package = "vyre-macros"
path = "vyre-macros"
owner = "registration-macros"
layer = "foundation"
responsibility = "Provide compile-time registration and declaration macros without depending on runtime crates."

[[crate]]
package = "vyre-primitives"
path = "vyre-primitives"
owner = "primitive-library"
layer = "primitives"
responsibility = "Own reusable Tier 2.5 program builders shared by higher-level libraries and runtimes."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-test-support"
path = "vyre-test-support"
owner = "test-support"
layer = "test-tooling"
responsibility = "Provide shared deterministic fixtures and assertions for workspace tests."

[[crate]]
package = "vyre-self-substrate"
path = "vyre-self-substrate"
owner = "self-substrate"
layer = "scheduler"
responsibility = "Use Vyre primitives to implement scheduler, graph, coverage, and optimization support."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate]]
package = "vyre-conform-spec"
path = "conform/vyre-conform-spec"
owner = "conformance"
layer = "conformance"
responsibility = "Define conformance case, result, and certificate schemas against the public facade."

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-conform"
path = "conform/vyre-conform"
owner = "conformance"
layer = "conformance"
responsibility = "Execute production artifacts against independent reference semantics, minimize counterexamples, check algebraic laws, and issue versioned certificates and replay records through one library and thin CLI."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-conform-spec"
purpose = "versioned conformance schemas"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "conformance"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-metal"
purpose = "native Apple backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "metal-driver"

[[crate.dependency]]
package = "vyre-driver-reference"
purpose = "reference backend adaptation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-intrinsics"
purpose = "hardware-mapped intrinsic builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "hardware-intrinsics"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["crypto", "full", "logical", "matching", "math", "nn", "nn-moe", "parsing", "security", "visual"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "xtask"
path = "xtask"
owner = "release-tooling"
layer = "tooling"
responsibility = "Generate evidence and enforce repository, release, documentation, and architecture contracts."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-bench"
purpose = "benchmark workloads and evidence"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "benchmarks"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-reference"
purpose = "reference backend adaptation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-driver"

[[crate.dependency]]
package = "vyre-driver-spirv"
purpose = "SPIR-V backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "spirv-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-emit-metal"
purpose = "native Apple source emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "metal-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-intrinsics"
purpose = "hardware-mapped intrinsic builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "hardware-intrinsics"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["crypto", "full", "logical", "matching", "math", "nn", "nn-moe", "parsing", "security", "visual"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-lints"
purpose = "source policy enforcement"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lint-policy"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "neutral artifact compilation and target payload contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["bitset", "fixpoint", "graph", "hash", "inventory-registry", "label", "matching", "math", "nn", "parsing", "predicate", "reduce", "text"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-runtime"
path = "vyre-runtime"
owner = "runtime"
layer = "runtime"
responsibility = "Own compile-to-materialize orchestration, artifact sessions, recovery, persistence, residency, scheduling, caches, telemetry, readback, and IO."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-self-substrate"
purpose = "optional scheduler and analysis substrate"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = true
default_features = true
boundary = "private"
seam = "self-substrate"

[[crate]]
package = "vyre-safetensors"
path = "vyre-safetensors"
owner = "safetensors-adapter"
layer = "runtime"
responsibility = "Validate safetensors metadata, shard indexes, compiler requirements, trusted shard digests, and immutable checkpoint identities without owning runtime residency."

[[crate]]
package = "vyre-libs"
path = "vyre-libs"
owner = "product-libraries"
layer = "libraries"
responsibility = "Own product-facing Tier 3 program compositions built from neutral primitives and contracts."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = ["serde"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["bitset", "decode", "fixpoint", "geom", "graph", "hash", "inventory-registry", "label", "matching", "math", "nfa", "nn", "opt", "parsing", "predicate", "reduce", "text", "topology", "visual"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "public"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "specification"

[[crate]]
package = "vyre-intrinsics"
path = "vyre-intrinsics"
owner = "hardware-intrinsics"
layer = "intrinsics"
responsibility = "Own registered hardware-mapped intrinsic contracts and their neutral program builders."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate]]
package = "vyre-aot"
path = "vyre-aot"
owner = "aot-artifacts"
layer = "packaging"
responsibility = "Plan and package ahead-of-time artifacts without owning live backend execution."

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-megakernel"
purpose = "whole-graph compilation and immutable artifact contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "megakernel-compiler"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["bitset", "fixpoint", "graph", "inventory-registry", "math", "reduce"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-frontend-rust"
path = "vyre-frontend-rust"
owner = "rust-frontend"
layer = "frontend"
responsibility = "Lower the supported Rust frontend subset into typed Vyre programs without owning backend execution."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate]]
package = "vyre-bench"
path = "vyre-bench"
owner = "benchmarks"
layer = "tooling"
responsibility = "Own reproducible workload benchmarks, comparisons, budgets, and raw benchmark evidence."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-driver"
purpose = "backend-neutral target, materialization, submission, and completion contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "backend-contract"

[[crate.dependency]]
package = "vyre-driver-cuda"
purpose = "native accelerator backend execution"
features = []
conditions = ["cfg(not(target_os = \"macos\"))"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "cuda-driver"

[[crate.dependency]]
package = "vyre-driver-metal"
purpose = "native Apple backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "metal-driver"

[[crate.dependency]]
package = "vyre-driver-reference"
purpose = "reference backend adaptation"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-driver"

[[crate.dependency]]
package = "vyre-driver-spirv"
purpose = "SPIR-V backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "spirv-driver"

[[crate.dependency]]
package = "vyre-driver-wgpu"
purpose = "portable backend execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "portable-driver"

[[crate.dependency]]
package = "vyre-emit-ptx"
purpose = "primary binary backend text emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-binary-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-frontend-rust"
purpose = "Rust source lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "rust-frontend"

[[crate.dependency]]
package = "vyre-intrinsics"
purpose = "hardware-mapped intrinsic builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "hardware-intrinsics"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["c-parser", "nn-linear-4bit"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "lowering"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = ["bitset", "cpu-parity", "graph"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate.dependency]]
package = "vyre-reference"
purpose = "independent semantic oracle execution"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "reference-semantics"

[[crate.dependency]]
package = "vyre-runtime"
purpose = "artifact admission, residency, submission, recovery, and readback lifecycle"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "runtime"

[[crate.dependency]]
package = "vyre-spec"
purpose = "stable cross-engine schemas and operation definitions"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "specification"

[[crate]]
package = "vyre-lints"
path = "vyre-lints"
owner = "lint-policy"
layer = "tooling"
responsibility = "Enforce source-level project policies without depending on runtime crates."

[[crate]]
package = "vyre-lower"
path = "vyre-lower"
owner = "lowering"
layer = "lowering"
responsibility = "Consume verified semantic programs and own the single backend-neutral lowering boundary and pre-emission transforms."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate]]
package = "vyre-emit-naga"
path = "vyre-emit-naga"
owner = "primary-text-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit the primary text representation and related binary targets."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-ptx"
path = "vyre-emit-ptx"
owner = "primary-binary-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit the primary binary backend text artifact."

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-spirv"
path = "vyre-emit-spirv"
owner = "spirv-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit SPIR-V artifacts through the shared writer."

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-emit-metal"
path = "vyre-emit-metal"
owner = "metal-emitter"
layer = "emitter"
responsibility = "Consume verified lowering products and emit native Apple shader source through the shared emitter path."

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate]]
package = "vyre-debug"
path = "vyre-debug"
owner = "debugging"
layer = "tooling"
responsibility = "Inspect, explain, and diagnose typed programs, lowering, and product-library composition."

[[crate.dependency]]
package = "vyre"
purpose = "public lifecycle facade"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "public-facade"

[[crate.dependency]]
package = "vyre-emit-naga"
purpose = "primary text and related binary emission"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "primary-text-emitter"

[[crate.dependency]]
package = "vyre-foundation"
purpose = "typed IR, graph, diagnostics, validation, and semantic optimization contracts"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "foundation-ir"

[[crate.dependency]]
package = "vyre-libs"
purpose = "product operation builders"
features = ["c-parser"]
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "private"
seam = "product-libraries"

[[crate.dependency]]
package = "vyre-lower"
purpose = "verified backend-neutral representation lowering"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = true
boundary = "public"
seam = "lowering"

[[crate.dependency]]
package = "vyre-primitives"
purpose = "reusable semantic Program builders"
features = []
conditions = ["always"]
kinds = ["normal"]
optional = false
default_features = false
boundary = "private"
seam = "primitive-library"

[[crate]]
package = "vyre-grammar-gen"
path = "vyre-grammar-gen"
owner = "grammar-generation"
layer = "tooling"
responsibility = "Generate host-side grammar tables consumed by frontend and parsing crates."
