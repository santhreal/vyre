# Recursive Composition Thesis

**Status: Superseded**

This document described an earlier `vyre-driver::self_substrate` layout. The
workspace now owns reusable scheduling and graph operations in
`vyre-self-substrate`, while linked operation contracts and composition chains
come from [`docs/generated/OP_SCHEMA.json`](generated/OP_SCHEMA.json).

Read [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) for current crate boundaries and
[`docs/RUNTIME_PIPELINE.md`](RUNTIME_PIPELINE.md) for the execution path.
