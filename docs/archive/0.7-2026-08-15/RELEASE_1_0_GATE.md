# Release 1.0 Gate Checklist

**Status: Superseded**

This checklist predated the generated release evidence and referenced
consumer-specific artifacts outside the Vyre repository.

Use [`docs/RELEASE_CHECKLIST.md`](RELEASE_CHECKLIST.md) for the maintained
operator checklist. The executable release contract comes from
`release/release-train.toml` and `cargo run -p xtask --bin xtask --
vyre-release-gate`.
