path = "scripts/lib/read_toml_values.py"
state = "retired"
reason = "toml_reader.sh reads the manifest subset itself in bash; the python3 it shelled into needed tomllib, which a 3.9 host does not have, and a failed import left a release loader with an unset tag"
