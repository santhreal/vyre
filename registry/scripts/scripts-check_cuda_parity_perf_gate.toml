path = "scripts/check_cuda_parity_perf_gate.sh"
state = "retired"
gate = "cuda-parity"
reason = "cuda-parity derives the vyre-driver-cuda test roster from tracked targets, requires a live device and at least one gpu_parity target, then runs the crate suite"
