lane = "gpu-parity-wgpu-conformance-all-ops"
workflow = ".github/workflows/gpu-parity.yml"
job = "wgpu-conformance-all-ops"
state = "live"
required_status = "GPU Parity + Determinism / WGPU conformance all ops"
triggers = ["pull_request", "push", "workflow_dispatch"]
steps = [
  { command = "./cargo_full run -p vyre-conform --release --features gpu --bin vyre-conform -- dispatch --backend wgpu --ops all" },
]
