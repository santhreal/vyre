name = "xtask-evidence"
concern = "Own the xtask subcommands that decide whether a recorded benchmark or release measurement still describes this tree."
pages = { spec = "xtask-evidence/SPEC.md", readme = "xtask-evidence/README.md" }
tests = { placement = "tests", inline_exceptions = [] }
depends_on = ["vyre-bench", "vyre-driver", "vyre-foundation", "vyre-registry-link", "xtask"]
