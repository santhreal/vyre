name = "vyre-conform"
concern = "Execute production artifacts against independent reference semantics, minimize counterexamples, check algebraic laws, and issue versioned certificates and replay records through one library and thin CLI."
pages = { spec = "conform/vyre-conform/SPEC.md", readme = "conform/vyre-conform/README.md" }
tests = { placement = "tests", inline_exceptions = [] }
depends_on = ["vyre", "vyre-conform-spec", "vyre-driver", "vyre-driver-cuda", "vyre-driver-wgpu", "vyre-foundation", "vyre-libs", "vyre-megakernel", "vyre-primitives", "vyre-reference", "vyre-registry-link", "vyre-runtime", "vyre-spec"]
