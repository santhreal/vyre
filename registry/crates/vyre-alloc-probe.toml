name = "vyre-alloc-probe"
concern = "Count heap traffic per thread behind a GlobalAlloc wrapper, so an allocation budget measures the code under test rather than the test schedule. Depends on no vyre crate, so a harness binary links it at run time and a driver suite links it as a dev-dependency."
pages = { spec = "vyre-alloc-probe/SPEC.md", readme = "vyre-alloc-probe/README.md" }
tests = { placement = "tests", inline_exceptions = [] }
depends_on = []
