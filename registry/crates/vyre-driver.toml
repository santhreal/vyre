name = "vyre-driver"
concern = "Define backend-neutral device, target compiler registration, artifact materialization, binding, submission, completion, capability, dispatch, and evidence contracts."
pages = { spec = "vyre-driver/SPEC.md", readme = "vyre-driver/README.md" }
tests = { placement = "tests", inline_exceptions = [] }
depends_on = ["vyre-foundation", "vyre-libs", "vyre-megakernel", "vyre-spec"]
