name = "structure-gate"
concern = "Enforce the crate roster, one operation identity per semantic operation, one home per concept, and one place per module. Depends on no vyre crate so it keeps running while the workspace does not compile."
pages = { spec = "structure-gate/SPEC.md", readme = "structure-gate/README.md" }
tests = { placement = "tests", inline_exceptions = [] }
depends_on = []
