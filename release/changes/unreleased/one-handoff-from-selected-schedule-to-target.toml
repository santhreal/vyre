category = "Added"
text = "Neutral lowering projects the selected schedule phase into a versioned record of frozen execution facts that every target reads, and emission refuses an entry point whose recorded geometry disagrees with it."
