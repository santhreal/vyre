category = "Fixed"
text = "example_capability panicked when a tracked example source could not be read, so a gate whose job is to report a finding aborted the sweep instead. An undocumented panic in xtask is itself a hygiene-matrix release blocker, and the unreadable file is now a Finding naming the path and the io error."
