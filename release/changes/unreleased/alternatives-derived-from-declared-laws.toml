category = "Added"
text = "Region alternatives are derived from the algebraic laws a combine registers and composed by a bounded expansion over an expression e-graph, so a declared law contributes rewrites without an operation-specific recipe."
