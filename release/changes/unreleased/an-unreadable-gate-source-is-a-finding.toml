category = "Fixed"
text = "A tracked gate source the working tree cannot read is a gate canon finding naming the file rather than an error that aborts the gate, so the rest of the registry is still judged and a ratchet constant nobody could read is not silently unjudged."
