category = "Fixed"
text = "Megakernel selection prices a launch at the device's measured per-launch overhead and traffic at the recorded 3788 bytes per nanosecond, so a fusion is ranked against what a launch costs on the target host instead of against a rate a thousand times below the one the benchmark recorded."
