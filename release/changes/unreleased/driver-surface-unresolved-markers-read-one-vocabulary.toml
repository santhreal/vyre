category = "Fixed"
text = "The backend feature-marker release blocker reads `tbd` again through the one owned unresolved-work vocabulary, which the tree-wide hygiene scan cannot carry because the conformance certificate schema states `TBD` as a field value."
