category = "Fixed"
text = "Materialized artifact execution preserves every mutable carrier across multi-segment dispatches and resolves fused-module resources by authenticated identity. Schema version 7 records canonical retained-predecessor lineage plus named entry input and output bindings. `InstanceCore` derives module resources from exact target binding metadata, updates antecedent retained values after dispatch, and rejects missing or mismatched resource identities instead of falling back to positional descriptor order."
