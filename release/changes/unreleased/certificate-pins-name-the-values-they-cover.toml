category = "Fixed"
text = "The conformance certificate regression pin passes, and a future drift in it is attributable. Seven pinned values had been stale long enough that the gate was red on every checkout: the region-chain bundle carries the test operation id in an `Expr::call`, so respelling that id from `vyre-conform.test.identity_u32` to `vyre_conform_test::identity_u32` when the operation and target registries were unified put one more byte in its wire body, 324 to 325, and moved its digest; and all five signatures had moved because the signable body covers `reference_output_blake3`, the digest of what the reference returns, which a wire-identical program can change underneath. Each new signature was reproduced independently from the bundle digest, the corpus digest, and the single output word each program stores before being written down. The test now asserts those words through the same planning and interpreter entry point the issuer uses, so a signature that moves while the words hold is a framing change and one that moves with them is a semantic one, where before either read as an opaque hash mismatch. The `vyre-conform` pin generator `_compute_pins.rs`, a second copy of all five bundle builders and the signing body whose only output was the constants to paste, is deleted: a generator that can drift from the test it feeds prints pins for programs nobody is checking, and the failing assertion already names the value to write."
