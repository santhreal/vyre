category = "Added"
text = "Four subsets group the gates no workflow named: `composition`, `structure`, `docs` and `ir`. The gates workflow runs each as its own step, so a red gate is addressed to the owner of that domain instead of arriving inside one whole-registry log. The whole-registry sweep stays as the backstop for a gate that belongs to no subset."
