category = "Changed"
text = "A second name for a composition witness is an alias, not a re-typed signature. The CSR forward-or-changed adapter and the numerical kernel pipeline tests re-export the vyre-reference witnesses under their local names instead of forwarding through hand-written twelve-parameter wrappers."
