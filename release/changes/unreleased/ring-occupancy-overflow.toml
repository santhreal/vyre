category = "Fixed"
text = "Ring occupancy sums report an overflowing slot count instead of saturating to a plausible total, and autotune cost selection carries the empty candidate set in its return type instead of a debug assertion absent from release builds."
