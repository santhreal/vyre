category = "Fixed"
text = "Release conformance reads the evidence line the conformance runner writes. The runner names the executor that produced a row, because the reference oracle answers from the interpreter and registers no device, while the recorded artifact keeps the column its readers index. The reader expected the artifact column on the wire and rejected every line, reporting a run in which every operation passed as a runner emitting invalid JSON with zero recorded op pairs."
