category = "Added"
text = "Candidate generation derives plans from a versioned schedule grammar whose seventeen productions cover every schedule transform, so one semantic graph produces structurally different kernel organizations for different device facts instead of the baseline, one fused edge, one greedy grouping and four launch widths."
