category = "Fixed"
text = "The implementation-family taxonomy is closed against the live operation registry. A row naming an operation no registration publishes now fails the registry contracts, and the two rows that named a withdrawn scaling primitive and a builder that emits no part of its claimant's body are gone."
