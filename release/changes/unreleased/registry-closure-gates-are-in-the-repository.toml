category = "Added"
text = "Every registry closure gate in the workspace is a tracked test. The gate asserts that each `pub fn ... -> Program` builder a crate publishes is reachable from its `inventory` registry or named by one of its tests, that the source enumeration finds at least a declared floor, and that a builder listed as an exception has not since gained coverage, which is what keeps the exception list only-shrinkable. Nineteen crates ran that gate from a file no checkout had: a blanket ignore rule for `<crate>/tests/adversarial_registry_closure.rs` kept each copy out of the repository, so the gate existed on one machine, ran in no continuous-integration job, and appeared in no review. Seven of those crates publish builders and now carry `<crate>/tests/registry_closure.rs` with that crate's floor and exception list; the other twelve enumerate no builders at all, so their gate could only ever pass, and a target that cannot fail is worse than none because it reads as coverage. Two of the invisible gates were failing. `vyre-driver` left the three hostile-input probe programs every backend's obligations are asserted against unregistered, untested and unlisted; those probes are now pinned by a contract target, because a probe whose buffer count or workgroup shape drifts leaves the backend assertions green while an over-supplied buffer case becomes the correct call and a zero workgroup dimension launches. `vyre-primitives` left three builders uncovered and unlisted; each is a second call convention over a builder that is swept, and each now says so on its own line. The same ignore rule also hid the lexer oracle's hostile-input half, which asserts that bytes outside the accepted subset fail loudly instead of tokenizing to a wrong answer; it is tracked under a name that says what it drives."
