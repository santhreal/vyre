category = "Added"
text = "`vyre-test-support` declares `test_payload_expr_extension!` and `test_payload_node_extension!`, which build an extension fixture whose fingerprint is the digest of its wire payload."
