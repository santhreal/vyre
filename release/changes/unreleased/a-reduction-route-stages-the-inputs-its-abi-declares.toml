category = "Fixed"
text = "Each route of the crossover reduction case stages the host input buffers its own program declares, so the fused tree route whose output the backend allocates is no longer handed an input it never declared."
