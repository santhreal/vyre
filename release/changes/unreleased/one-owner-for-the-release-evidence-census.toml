category = "Fixed"
text = "The release gate judges the evidence census through the module that writes it, so the schema, the required generators and the artifact rules are stated once. The second reader still asked for a per-command spawn status and two counters retired in schema 5, which reported every generator as failed and printed an absent counter as the u64 maximum."
