category = "Added"
text = "Adaptive traversal publishes the nine buffer names one step binds as `AdaptiveTraversalBuffers::CANONICAL`, with canonical names for the active-source queue and its resident length."
