category = "Added"
text = "`vyre_foundation::visit::ExprChildren::is_empty` and `vyre_foundation::visit::expr_is_leaf` state whether an expression has operands, and `LogicalExchangeKind::ALL` is the whole variant set as a fixed-length array. Three traversal sites derived the same answer from a child walk."
