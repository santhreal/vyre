category = "Added"
text = "`vyre::visit` exports the IR traversal module. `Expr` and `Node` are `#[non_exhaustive]`, so a caller outside `vyre-foundation` cannot match them exhaustively; the facade published both types and no way to descend through them, which left a complete walk impossible through the public surface alone."
