category = "Added"
text = "Schedule selection places a program on the device mesh the request states, recording one topology with per-region partitions, shard coordinates and transfers, one target payload per placed device, and a runtime session that submits that mesh exactly or refuses it."
