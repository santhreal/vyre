category = "Changed"
text = "The persistent autotune store holds the unroll and tile a measured variant was compiled at, no longer persists a launch width no artifact authorized, and rejects a file written under a different schema instead of serving it as current evidence."
