category = "Fixed"
text = "The ci-steps gate reads every script under scripts/, including the shared shell functions and readers in scripts/lib that the registry declares and every script sources; a single-level read skipped them while reporting the directory covered. Cargo invocation resolution moved into that gate, which reads workflows and manifests rather than Rust source text, so its structural-gate row is deleted rather than re-pointed."
