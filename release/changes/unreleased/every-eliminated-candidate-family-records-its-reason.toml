category = "Added"
text = "Constraint propagation eliminates a derived candidate before anything is generated for it, and the artifact records a search certificate holding the grammar version, the depth reached, every derived family with its admitted count, every eliminated family with one of thirteen stable reason codes, and whether a bound stopped the search."
