category = "Changed"
text = "The line scanner that separates code from a comment, counts nesting, and decides which lines belong to an inline test module lives in the scan module that already owns what is not code. The hot-path scan held the only copy, so the heuristic audit could not tell a test fixture from production debt."
