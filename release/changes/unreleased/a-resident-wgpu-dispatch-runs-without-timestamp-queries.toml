category = "Fixed"
text = "A WGPU resident-handle dispatch runs on an adapter that carries no timestamp query and reports `device_ns` as `None`, instead of refusing the dispatch. The refusal made every retained execution through `launch_resident` fail on such an adapter; `dispatch_borrowed_timed`, where the caller asks for device time, still refuses."
