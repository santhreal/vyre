category = "Fixed"
text = "The compiler-grade thesis axes and the CPU-SOTA 100x contract page name the workloads the suite measures: AST motif traversal replaces the deleted C parser axis, e-graph saturation points at workload 17, and the required 100x case list matches the ten cases the release thresholds declare. The two superseded e-graph artifacts are deleted and the cross-backend comparison table is regenerated from what remains."
