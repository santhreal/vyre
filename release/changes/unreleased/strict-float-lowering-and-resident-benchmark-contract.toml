category = "Fixed"
text = "Every registered backend either honors strict-IEEE float lowering or refuses the dispatch with an actionable diagnostic naming the mode and the unexpandable or approximable operations, closing row 142 across CUDA, Metal, SPIR-V, and WGPU drivers. The resident optimizer pipeline benchmark contract and evidence are aligned with the measured floor, and bank conflict strategy resolution contracts are validated across all backends."
