category = "Fixed"
text = "A quantized, graph, or solver dispatch readback failure reports its byte-count and buffer-count contract once instead of behind two copies of the semantic execution prefix."
