category = "Fixed"
text = "Loop-invariant hoisting has one owner, so the resident pipeline no longer hoists a binding whose name a sibling loop also binds and produces a program the validator rejects, and a load from a read-only buffer now leaves the loop under the pass engine as well."
