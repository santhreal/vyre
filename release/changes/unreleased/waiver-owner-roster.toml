category = "Fixed"
text = "The hand-written-descent waiver roster holds each row to the owner docs/CRATE_OWNERSHIP.toml declares for the crate containing the file, instead of a compiled-in list of three names the tree never used."
