category = "Fixed"
text = "Release benchmark evidence now records the optimizer-impact performance contract and explicit zero-launch PTX proof metrics. Resident string-bitmap batching reads back one canonical bitmap while retaining the full device-resident batch, avoiding redundant PCIe output transfers."
