category = "Changed"
text = "An admitted target module submits the grid, workgroup, coverage and shared bytes the artifact recorded for its own entry point, and a payload-stated geometry never reaches a submission."
