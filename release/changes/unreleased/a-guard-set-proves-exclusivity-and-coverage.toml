category = "Added"
text = "A specialized portfolio is admitted only when its guards are provably disjoint or ordered by precedence and every declared workload cell is served by a variant or by a generic remainder."
