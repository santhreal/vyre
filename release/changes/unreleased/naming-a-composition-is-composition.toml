category = "Fixed"
text = "A region that names the operation behind it counts as composed. Making every library operation tag its own entry region added one node that the composition measurement read as own work, so the measured fraction of every operation that took that fix went down and two ops reported a composition regression they had not suffered. The trend baseline is unchanged; the measurement is."
