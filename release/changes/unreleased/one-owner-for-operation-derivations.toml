category = "Changed"
text = "A registration and the semantic record derived from it now share one definition of the canonical program, the composed schedule constraints, the local effects and capabilities, and the byte order those are hashed into a composite version with."
