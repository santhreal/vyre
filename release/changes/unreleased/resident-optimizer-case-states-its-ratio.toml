category = "Fixed"
text = "The resident optimizer pipeline benchmark header states the ratio the case records rather than a speedup, matching the 0.10 floor and the release target that already state the device arm is slower than the host arm at 50000 bindings."
