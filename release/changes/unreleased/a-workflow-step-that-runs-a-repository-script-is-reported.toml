category = "Fixed"
text = "A workflow step that runs a repository script is reported, since every continuous integration assertion is a registered gate and a script invocation carries an assertion the registry, the baseline and the subset roster cannot see."
