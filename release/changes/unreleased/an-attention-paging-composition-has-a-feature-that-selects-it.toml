category = "Added"
text = "vyre-libs-encoding declares an nn-paging feature that selects the attention paging composition. The module was gated on four feature names the crate does not declare, so no configuration compiled it."
