category = "Fixed"
text = "Law-derived candidates are bounded by the request's candidate budget, so a graph the law tables reach at more nodes than the budget grants candidates no longer compiles to an artifact that admission refuses for exceeding its authenticated search work."
