category = "Added"
text = "The benchmark protocol provides resumable measurement campaigns with deterministic trial randomization, refuses averaging across incompatible cells by name, and requires empirical backing proofs for performance floor records."
