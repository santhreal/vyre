category = "Added"
text = "Conformance compares every schedule family a semantic graph reaches against the unspecialized baseline under an exact or bounded unit-in-last-place contract the operation declares."
