category = "Fixed"
text = "The multi-workgroup u32 tree reduction sizes its grid and its partial buffer to the launch its own buffer table implies, so no workgroup reduces elements the combine pass does not read."
