category = "Added"
text = "CompileRequest::with_numeric_budget states the error a caller admits, and a schedule that reorders a rounding accumulation is admitted where the reordered contract fits that budget instead of being eliminated."
