category = "Fixed"
text = "The consumer-neutrality gate no longer carries a roll call of seventeen documents, fifteen of which the documentation collapse deleted, each reported as a finding on every tree. The scanned set is enumerated from the tree, which already covered the two survivors and covers every document added since, so the gate reaches zero by describing what is there rather than by mourning what is not."
