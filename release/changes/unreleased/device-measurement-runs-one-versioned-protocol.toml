category = "Changed"
text = "Budgeted device measurement runs one versioned protocol covering warmup, rotated candidate interleaving, a trimmed-median estimator with uncertainty, a stopping rule and an equivalence band, and the artifact retains every candidate's raw samples, prediction error and reported device state."
