category = "Added"
text = "`MemoryKind::ALL` and `BufferAccess::ALL` list every memory tier and access mode as fixed-length arrays, so a crate that cannot match those non-exhaustive enums exhaustively can still walk the whole space."
