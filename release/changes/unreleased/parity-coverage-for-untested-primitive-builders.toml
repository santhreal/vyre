category = "Added"
text = "Every published FNV-1a64 program builder, the slot-precise `arg_of_slot` traversal, and both queued-row CSR traverse delegating forms now have parity coverage. The FNV-1a64 and delegating-form member sets are derived from the source at run time, so a new member fails the suite instead of shipping unproven."
