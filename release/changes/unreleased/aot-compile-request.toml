category = "Added"
text = "`vyre-aot::compile` is the crate's one compile entry point and accepts a validated compiler request plus a registered target identity, so ahead-of-time compilation consumes the caller's graph, external facts, device facts, objective and search budget without inventing a default."
