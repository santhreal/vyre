category = "Added"
text = "`vyre-aot` exposes `compile_request` accepting a validated compiler request and registered target identity, allowing AOT compilation to consume the canonical `ValidatedCompileRequest` without inventing default parameters."
