category = "Changed"
text = "vyre-megakernel is the single schedule-selection owner across fusion, geometry, topology, persistence, layout, and placement. Foundation retains semantic analyses and neutral facts only, while drivers report capabilities and runtime submits the selected artifact without post-freeze retuning."
