category = "Fixed"
text = "The composition complexity budget now holds across all registered library operations without exemptions. Eleven operations exceeding complexity limits (AST CSE hashing, Shunting-Yard, planar rewrite scheduling, CSR queue traversal, CSR forward or changed, dominator frontier, dominator tree LCA intersection, tensor flow edge propagation, VAST postorder walk, variance reduction, and online softmax attention) are factored into genuine registered sub-operations with deterministic fixtures, and the variance reduction tree uses a bounded step loop rather than unrolled duplicate branches."
