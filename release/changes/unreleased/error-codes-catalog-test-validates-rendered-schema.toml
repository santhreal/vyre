category = "Quality"
text = "`xtask-registry` tests the `error-codes` gate output by validating that both the driver and validation error catalogs produce valid TOML containing all declared variants and rules with header regeneration comments, replacing a self-equality assertion."
