category = "Changed"
text = "Thirty-two structural gates that read the checkout are declared in the structural-gate registry with the property each asserts and why no running program witnesses it, covering the transform classification table, the child-slot vocabulary, checkout provenance, grid-sync forwarding, the vyre-libs duplication budget, resident-layout ownership, graph dispatch wrappers, bench metric and payload ownership, CUDA trap reporting, expression-type ownership, async rule coverage, node-visitor hooks and implementors, scalar operator agreement, cast narrowing coverage, backend registration declarations, wgpu instance ownership, xtask exit causes, test write triggers, descriptor proof identity, and CI selector resolution. Two rows the tree no longer backs are deleted."
