category = "Fixed"
text = "The paged key-value cache folds a physical block id read from the block table into the declared block count, so a block-table entry at or past the end of the cache reads and writes block zero instead of an address past the cache buffer."
