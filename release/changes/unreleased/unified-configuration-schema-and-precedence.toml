category = "Added"
text = "The configuration schema is unified into a typed descriptor substrate partitioned into semantic compile inputs, operational policy, diagnostic controls, and credentials with explicit precedence resolution and strict secrecy redaction."
