category = "Added"
text = "Schedule selection records one versioned allocation and layout plan that lowering verifies its bindings against, the runtime allocates and binds exactly, and device-reported resident bytes are reconciled with."
