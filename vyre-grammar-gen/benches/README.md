# vyre-grammar-gen benchmarks

Benchmark harnesses for grammar generation live here. This tree owns token
table generation throughput, emitted grammar size, and parser-table stability
measurements used by distributed parser release evidence.
